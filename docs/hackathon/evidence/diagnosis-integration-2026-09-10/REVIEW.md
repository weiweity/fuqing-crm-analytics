# 数值接线增量审查

基线为 8c2e676；审查当前未提交的合同、计算/存储、HTTP、来源适配、UI、生成物和回归。沿 gstack /review 清单检查 SQL、身份与持久化、结果类型分派和窗口语义；未采用独立代理或外部模型审查。原 #115 代码与 CI 是独立前置证据，不把其 CI 标作本增量远端通过。

范围：D1–D4 已实现；D5 的计算/浏览器/有界真实模型和备份部分通过，原生取消期间不发布的验证及 Git 交付尚未完成。完整产品目标没有缩减。

已修复的具体问题：

- P1，置信度 10/10：计算从已加载测试模块发现退款文件，导致相同库随 import 改变结果或触发只读写入。已删除运行时补库，fixture 显式写退款；修复前失败证据见 computation-isolation-verification.json。
- P1，置信度 10/10：新计算结果添加到已有板时用 run_id 代替 result_id。现在保留结果标识，重开拒绝与快照不一致的 result/run 绑定；HTTP 添加、复制、撤销和损坏回归通过。
- P1，置信度 10/10：快照存储未验证计算标识与 actor/session/request 的对应。保存和重读均校验；跨请求保存、数据库绑定损坏负测通过。
- P2，置信度 10/10：旧 T04 源码正则禁止所有乘 100，无法区分 GSV 金额、raw ratio 与柱宽。替换为实际编译组件断言，金额 0.25 和比例 25.00% 均验证；旧 HTTP 夹具补齐冻结合同。

增量复查：P1，置信度 10/10，实际 Node transport 中断后保存仍继续，已由真实 socket/受控计算屏障复现，见 cancellation-before.txt。新增诊断取消端点与持久化标记，和保存共用 SQLite 写事务；回执失联不报成功，保存先完成不撤销。实际 Node 手动取消与 5 秒超时、跨进程、权限/会话隔离、写锁及已发布回归均通过。SQL 参数化、身份绑定、状态兼容及超时边界复查未发现本增量未处理的 P0/P1。

结果：**PASS（本地代码增量）**。完整 backend 2320 passed / 77 skipped，独立副本完整 B0 PASS，源码逐文件核对一致，见 cancellation-verification.json。既有两条真实模型 GSV 和模型结果成板/重开证据继续有效；没有为取消测试新增模型调用。浏览器停止按钮到宿主 signal 的计算中全路径仍未新跑，不标为完整 T17；外层主题、完整诊断、旧 MCP、业务默认值、T15/T16 和正式发布继续按七阶段账本处理。Git 交付以该 PR 实际提交和 CI 为准。
