# 提交前增量审查

Scope Check: CLEAN。PR #115 的目标分支为 main；本次增量基于 24a3722，范围为金额单位来源、版本兼容、调用方显示和对应证据。主线已 fetch；此前分支的大量实现沿用已记录审查，本报告不声称本轮重新审阅整个分支。未启动 OCR、子 Agent 或跨模型审查。

Pre-Landing Review: No new code-blocking issues found.

- SQL/数据：没有新表或更新旧结果；ComputedResultStore 仍在保存和读取时校验完整模型与执行绑定。新进程读取和 HTTP 保存重开已验证单位未丢失。
- 版本/枚举：新增 v2 同步了事实模型、计算入口、生成类型和客户端 decoder；旧 v1 模型/夹具未改。能力目录只声明当前源，不能用于给旧结果补单位。检查了 store、资产投影、类型联合和图表调用方。
- LLM 边界：原生审计证明旧模型的单位断言没有来源。源只接受显式合成声明，金额单位受枚举与状态约束；方法包禁止借用其他合同或掩盖自动保存。模型输出效果尚未复测。
- 摘要/数值：单位在 evidence_payload 的 facts 内；篡改单位负测失败符合预期。金额不缩放，比例仍为原始比值；已知元/分和未知路径由实际后端生成的 UI 夹具覆盖。
- 前端：新旧版本均在严格解码后进入认可列表；单一单位标签用于结果选择及每种图表的共用表格，LINE 的可访问名称亦有单位。编译后 DOM 通过不替代浏览器视觉。
- 副作用：兼容性脚本使用实际固定旧/新模块和临时合成 SQLite，子进程使用明确 cwd/PYTHONPATH，无 shell 插值；没有触碰运行候选状态或归档数据。

本地验证见 verification.json：完整后端 2339 passed / 77 skipped；B0 pipeline PASS；受影响后端 47 passed；单位解码 5 passed。Ruff 和 diff check 通过。GitHub 两类评论查询没有 Greptile 评论。

计划分阶段执行：本地代码/合同/存储/DOM/兼容性完成；候选五库备份恢复、运行切换、视觉和真实模型复测未完成。已知旧读者拒绝 v2，是候选发布的前置条件，不阻止提交本地修复。未合并、未发布，不能将本报告视为整产品审核通过。
