# 集成候选本轮结果（T05 关闭 + A6/A7 HTTP + 独立 T17）

日期：2026-09-09。候选工作树 `competition-integration`，分支 `grok/competition-integration`。<br>
**未 Git 提交 / 未 push / 未发 PR。** 未付费模型调用、未容量测试、未读真实业务数据。未占用或停止用户演示 `127.0.0.1:4327`（始终 PID 81058）。

## 绑定代码版本

| 项 | 值 |
|---|---|
| 工作树 HEAD（base，未提交） | `71a65f06f87cb3a5cbd888372487b6b36faaf2a6` |
| 上游 DSH SHA | `d347e703908d0406b7a7ef80e3a0e594d86b2215` |
| C0 `contract_hash` | `97ee36833200b3a1626a4fa88e6b3bc9591bfb5f6bebc5e666aeabdcce13815a` |
| Node | `/Users/hutou/homebrew/opt/node@24/bin/node` v24.19.0 |
| 脏工作区 | `git diff --stat` 约 51 文件 + 未跟踪比赛模块（本轮未 commit） |

关键文件 SHA-256（本轮实测绑定）：

```
f56afce065fb777adbef15f998ab8a4b8d66c4c5adfb56904e915b3191abc6f9  backend/services/analytics/competition_audience/features.py
e69a2dc169e408efe328742f8acaf645a8f3832a5f99c91e20334b24bdd5b505  backend/analytics_competition_app.py
135566a9ba2877d040b96ffccaa9d9d671310ac2bce9297d4487c0b8bf4f7128  dsh-plugins/analytics-workbench/src/client/competition-http.mjs
f52fac621dddfa881cb805322f548a7e71f3edd719342d9805b9ccc7a58e2bc7  dsh-plugins/analytics-workbench/src/client/competition-actions/transport.mjs
ff8303eae28b8f68cef314cd488a551d49c5cc3a8c37e727cfac337e18a2e162  dsh-plugins/analytics-workbench/src/client/competition-board/transport.mjs
75f1fb6edea966f7e63d98e0a21656e1fa40371dda27e791c5b814a7c5c88b13  dsh-plugins/analytics-workbench/src/competition-agent/tools.mjs
8619d77885c22df93c82095fdaf07024abf44253b447260bd50b39981575435a  dsh-plugins/analytics-workbench/lib/client.js
```

## 可复现命令

工作树：

```bash
INT=/Users/hutou/Desktop/ai-engineering/历史项目/fuqin-date/.worktrees/competition-integration
A9=/Users/hutou/Desktop/ai-engineering/历史项目/fuqin-date/.worktrees/competition-a9
PY=/Users/hutou/Desktop/ai-engineering/历史项目/fuqin-date/fuqing-crm-analytics/.venv/bin/python
NODE24=/Users/hutou/homebrew/opt/node@24/bin/node
UPSTREAM=/Users/hutou/Desktop/ai-engineering/历史项目/fuqin-date/fuqing-crm-analytics/.context/dsh-b0/upstream
```

### T05 / HTTP 接线（TestClient，不占端口）

```bash
cd "$INT"
PYTHONPATH="$INT" PYTHON_DOTENV_DISABLED=1 \
  "$PY" -m pytest backend/tests/test_competition_audience_t05.py \
    backend/tests/test_competition_http_wiring.py \
    backend/tests/test_competition_audience.py -q
# 28 passed

cd "$A9"
A9_SUT_ROOT="$INT" PYTHON_DOTENV_DISABLED=1 \
  "$PY" -m pytest backend/tests/test_competition_acceptance_golden.py \
    backend/tests/test_competition_acceptance.py -q
# 43 passed（含 test_t05_http_default_source_must_not_replace_a9_ids）
```

### A6/A7 传输单测

```bash
cd "$INT/dsh-plugins/analytics-workbench"
"$NODE24" --test src/client/competition-http.test.mjs \
  src/client/competition-board/competition-board.test.mjs \
  src/client/competition-actions/competition-actions.test.mjs \
  src/competition-agent/competition-agent.test.mjs
# 25 passed
```

### 合成 HTTP（18082，禁止 8000/4327/5173）

```bash
cd "$INT"
PYTHONPATH="$INT" PYTHON_DOTENV_DISABLED=1 \
  COMPETITION_SYNTH_PORT=18082 \
  COMPETITION_HTTP_TOKEN=b0-competition-synth-token-32chars \
  "$PY" scripts/competition-synth-http.py
```

未认证 `GET /api/v1/analytics/catalog` → 401。带 token：catalog 200（含 `diag.gsv`）；`POST .../diagnosis/capabilities` → `live_transport=HTTP_CONNECTED`；T05 A8 候选 `t05u05–t05u10`（6）；T05 A9 候选 `C05–C10`（6）。

### 插件构建（浏览器内联合成 HTTP，不指向 4327）

```bash
cd "$INT/dsh-plugins/analytics-workbench"
COMPETITION_HTTP_BASE=http://127.0.0.1:18082 \
COMPETITION_HTTP_TOKEN=b0-competition-synth-token-32chars \
  "$NODE24" ./build.mjs "$UPSTREAM"
```

### 独立 T17 DSH（只绑 14327）

```bash
cd "$INT"
"$NODE24" scripts/dsh-dev/cli.mjs diagnose --upstream "$UPSTREAM"
"$NODE24" scripts/dsh-dev/cli.mjs check --plugin off --web-port 14327 --upstream "$UPSTREAM"
# dump 含 NATIVE_WEB_IDS 全项；analytics-workbench-ui 不出现

COMPETITION_HTTP_BASE=http://127.0.0.1:18082 \
COMPETITION_HTTP_TOKEN=b0-competition-synth-token-32chars \
  "$NODE24" scripts/dsh-dev/cli.mjs start --plugin on --web-port 14327 \
    --upstream "$UPSTREAM" --fresh --detach

curl -sS -o /dev/null -w "%{http_code}" http://127.0.0.1:14327/   # 401
"$NODE24" scripts/dsh-dev/cli.mjs status
# 停本轨： "$NODE24" scripts/dsh-dev/cli.mjs stop
# 禁止对 4327/8000/5173 发 stop/kill
```

## 本轮判定

| ID | 层 | 状态 | 证据 |
|---|---|---|---|
| T05 | compute + HTTP | **PASS** | A8 `t05u*` 与 A9 `C01–C10` 按 `cohort_id` 分发；A9 HTTP 默认源不再用 A8 名单顶替；live 18082 同样 6≠4 |
| T01–T04, T06–T12, T14 | 既有 A9/集成 pytest | **PASS（此前+本轮复跑）** | A9 43 passed；集成 competition pytest 28+66 passed |
| A6 HTTP | transport + 浏览器 | **PARTIAL** | 路径改为 `/api/v1/analytics/competition/*`；UI 芯片包装成 C0 T05 body；浏览器 `GET 18082/.../results` 200。默认板 `dash_c0_board_a` 404（合成库未种子该板）。切换「认可成板」后原生 `<dialog>` 被关掉，板块未稳定留在视口 |
| A7 HTTP | tool execute | **PASS（合成）** | `competition_growth_capabilities` → `live_transport=HTTP_CONNECTED`，19 项；拒绝 4327/8000/5173。step 仍走夹具结果，**不是**真实模型 |
| T17 | 独立 DSH + 浏览器 | **PARTIAL** | 见下表 |
| T13 | 真实模型 | **NOT_RUN** | 明确跳过付费调用；onboarding 选 Configure later |
| T15 | 三角色 UAT | **NOT_RUN** | 需真实模型+业务签收 |
| T16 | 容量 | **NOT_RUN** | 本轮禁止 |
| Git 发布 | — | **NOT_RUN** | 未授权 |

### T17 原生矩阵（本轮独立 14327，plugin on）

| 项 | 状态 | 说明 |
|---|---|---|
| 启动/认证 | **PASS** | 未认证 GET `/` = 401；launch token→cookie；boundPorts 仅 `[14327]` |
| plugin off dump-config | **PASS（配置层）** | 18 个 `NATIVE_WEB_IDS` 均在 dump；未启动 plugin-off 长驻实例 |
| plugin on 运行 | **PASS** | `analytics-workbench` 已加载；驾驶舱含「认可成板 / 人群行动」 |
| 会话 session | **PASS（入口）** | New session / Search sessions / No sessions yet。未建长会话 |
| 设置 settings | **PASS** | Settings → General / Models / Plugins / Agent presets；Permission=Workspace Write |
| preset | **PASS（入口）** | Standard mode；`POST /api/agentPresets/list` 200 |
| 文件 files | **PARTIAL** | Add attachment 在未选 workspace 时 disabled；overlay 禁止复制 B0 对 directory-picker 的 disable |
| 权限 permissions | **PARTIAL** | 认证 401 保留；未跑撤权负测 |
| 三档 1440/1024/390 | **PASS（截图）** | `evidence/GROK/screenshots/` |
| 键盘 | **PARTIAL** | Tab 落到 New session；Esc 关 overlay 未单独记 |
| 品牌 | **PARTIAL** | 壳为「伸美 · B0」；Logo `/b0/brand/logo.png` 404（LFS 指针未 smudge）；问候语仍有上游 “Into the Unknown”；document.title 仍为 DeepSeek Harness |
| 图表等价表 | **NOT_RUN** | 成板视口未稳住 |
| 压缩 compaction | **NOT_RUN** | 无长会话、无真实模型 |
| 真实模型 | **NOT_RUN** | 无密钥、本轮不启 |
| 未 HTTP 探测 4327 | **PASS** | diagnose `did_not_http_probe_4327`；PID 81058 全程未变 |

## 剩余失败

1. **驾驶舱内切「认可成板 / 人群行动」后 `<dialog>` 关闭**（原生壳 light-dismiss / onClose 同步 store）。HTTP 已发出（`GET .../results` 200），但板块未留在视口。需再修 DSH overlay 关闭语义。
2. **合成库没有 `dash_c0_board_a`**：`GET /boards/dash_c0_board_a` 404，属诚实空态，不是假成功。
3. **B0 同域资产** `/b0/assets` `/b0/dashboards` `/b0/analyses` 在独立 DSH 上 404 → 叠加「资产请求失败」，比赛 HTTP 仍 CONNECTED。
4. **Logo LFS 指针**未 smudge：`/b0/brand/logo.png` 404。
5. **A3 MCP stdio / 4096 截断** 仍 PARTIAL（本轮未扩）。

## 待确认业务口径

C0 `defaults_pending_confirmation` 仍为：

- `sample_mode_when_excluding`
- `board_layout_mode`

另仍待业务确认（未在本轮假装通过）：

- 会员历史保持 `UNKNOWN`，不得用当前 `is_member` 回填
- 派样渠道集合 UNKNOWN
- antd 未锁版本
- 观察期未回购 ≠ 永久流失
- 零分母比例不得伪 0%（A9 T04 已要求 `safe_ratio(0,0)` 为 null）

## 停本轨实例（不要动 4327）

```bash
cd "$INT"
"$NODE24" scripts/dsh-dev/cli.mjs stop    # 只停 14327 本 supervisor
# 合成 HTTP：只结束监听 18082 的本轨 Python，禁止 kill 8000/4327
```
