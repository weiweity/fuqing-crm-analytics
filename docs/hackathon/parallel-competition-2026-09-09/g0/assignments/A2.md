# A2 第一批派发（只读口径核验，不改计算实现）

你是 A2（旧 CRM 计算与日期口径），不是总控。禁止再派子 Agent、禁止 commit/push/PR/merge、禁止切换/创建分支。

## 已填实的位置（不要猜）

- cwd：`/Users/hutou/Desktop/ai-engineering/历史项目/fuqin-date/.worktrees/competition-a2`
- 分支：`codex/competition-a2`
- base_sha：`71a65f06f87cb3a5cbd888372487b6b36faaf2a6`
- plan_snapshot_hash：`53f472f11c0ddfdf2106dd71fabe3c22c475a639a93962e95795931be8fb2937`
- contract_hash：`PENDING_C0`（本批不冻结合同；缺口提交给 A1/总控）
- 任务卡：`docs/hackathon/parallel-competition-2026-09-09/A2.md`

开工第一件事：核验实际 cwd/分支/HEAD/dirty。

## 本批必须完成

C0 尚未冻结，**禁止修改** `backend/services/metrics/`、`backend/services/rfm/`、semantic、customer_features 实现。本批只做：

1. 对照总计划 §12.1 / API-01/02 与测试计划 T01–T05、T14，只读核验当前日期、GSV、新老客、小样两条排除路径、F 订单粒度、R/as_of、销售 vs 历史 scope、会员/未知的**实际代码行为**。
2. 先追 CTE 粒度，记录 COUNT(*) 风险，不能还没读查询就判错。
3. 列出：已支持 / 部分支持 / 不支持 / 未知；HTTP 未暴露但 service 已支持的参数；派样渠道与会员历史若源码不够则报事实缺口，禁止猜测渠道集合。
4. 设计独立手算金标准夹具（合成数据，不读真实 DuckDB）：闰日、9/1 T+1 无本月数据、9/5 首购在 9/15–21 为老客、一订单三明细、跨渠道回购、未知会员、退款、只剔本期 vs 历史重算。把夹具写成 `backend/tests/test_competition_metrics*.py` 的**预期表/fixture**，测试可以先 skip 或只断言设计表存在，不要在本批“修好实现让测试绿”。
5. 输出给 A1 的 Schema 缺口 CR、给 A3 的需暴露参数清单、给 A8 的历史 cohort 特征接口需求。

可写：`backend/tests/test_competition_metrics*.py`、`docs/hackathon/parallel-competition-2026-09-09/evidence/A2/`。

本批结束 status 应为 `PARTIAL`（核验+夹具），不是计算实现 COMPLETE。
