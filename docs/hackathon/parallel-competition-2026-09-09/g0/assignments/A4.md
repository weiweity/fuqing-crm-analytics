# A4 第一批派发

你是 A4（DSH 基座、品牌壳与统一主题），不是总控。禁止再派子 Agent、禁止 commit/push/PR/merge、禁止切换/创建分支。

## 已填实的位置（不要猜）

- cwd：`/Users/hutou/Desktop/ai-engineering/历史项目/fuqin-date/.worktrees/competition-a4`
- 分支：`codex/competition-a4`
- base_sha：`71a65f06f87cb3a5cbd888372487b6b36faaf2a6`
- plan_snapshot_hash：`53f472f11c0ddfdf2106dd71fabe3c22c475a639a93962e95795931be8fb2937`
- contract_hash：`PENDING_C0`
- 任务卡：`docs/hackathon/parallel-competition-2026-09-09/A4.md`
- **必须完整阅读** 工作树内 `DESIGN.md` 与 `docs/hackathon/DSH-UI-INTERACTION-SPEC.md` 后再改 UI。

## 本批必须完成

1. 完善 `scripts/dsh-dev/` 启动/认证/陈旧构建/端口归属诊断说明或脚本（不要停止或复用用户进程）。
2. 冻结原生功能保留矩阵：会话/输入/流式工具/文件/权限/压缩/设置/preset/slots。逐项 PASS/FAIL/NOT_RUN。未实际启动验证的标 NOT_RUN，不得称全部原生功能已保留通过。
3. 用已有 Logo/字体和 Ant Design tokens 做比赛品牌壳：`dsh-plugins/analytics-workbench/src/client/competition-shell/` + 必要的 `src/client/styles.ts`。覆盖欢迎/导航/标题/错误/弹窗基础。不重画品牌、不覆盖固定上游源码、不关闭认证、不删原生能力。
4. 导出给 A6：`ThemeProvider`、`LayoutSlot`、公共状态/证据组件。在 `docs/hackathon/parallel-competition-2026-09-09/evidence/A4/theme-exports.md` 写清实际 export 名称。接入 `client/index.tsx` 由总控做，你只交接线说明。
5. 官方扩展可达性/缺口表、固定版本/许可记录。react-grid-layout 等候选只记录 SHA/许可/耦合，本批不要 `pnpm add`。

## 进程与依赖

禁止碰 127.0.0.1:4327 / 8000 / 5173。需要独立 DSH 时只用 14327，Vite 只用 15173，且本批默认不启动；若启动则结束必须停掉自己的 PID。禁止 `pnpm install` / `pipeline --prepare`。类型检查可只读复用主仓 `dsh-plugins/analytics-workbench/node_modules`。禁止改 `client/index.tsx`、依赖锁、看板业务组件、AI 工具。

截图：无独立运行实例则三档页面标 NOT_RUN，不要去用户 4327 截图冒充本轨证据。
