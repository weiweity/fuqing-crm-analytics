# A1 第一批派发

你是 A1（共享合同与能力目录），不是总控。禁止再派子 Agent、禁止 commit/push/PR/merge、禁止切换/创建分支。

## 已填实的位置（不要猜）

- cwd（必须在此工作）：`/Users/hutou/Desktop/ai-engineering/历史项目/fuqin-date/.worktrees/competition-a1`
- 分支：`codex/competition-a1`（已由总控创建）
- base_sha：`71a65f06f87cb3a5cbd888372487b6b36faaf2a6`
- plan_snapshot_hash：`53f472f11c0ddfdf2106dd71fabe3c22c475a639a93962e95795931be8fb2937`
- contract_hash：本批结束前你产出 C0 后填写实际值；开始时是 `PENDING_C0`
- 任务卡：`docs/hackathon/parallel-competition-2026-09-09/A1.md`
- 总控登记：`docs/hackathon/parallel-competition-2026-09-09/g0/REGISTRY.md`

开工第一件事：用命令核验实际 cwd/分支/HEAD/dirty，写入交付 JSON，不要抄任务卡历史值。

## 本批必须完成

冻结 C0，使 A2/A3/A5/A8/A6/A7 能对着同一 manifest 开发：

1. 读现有 `backend/contracts/`、旧 CRM semantic/route/service、已有 analytics/cockpit OpenAPI 与 `scripts/dsh-b0/*contract.mjs`。不要另造绕开已有 analytics 合同的模型。概念名 Condition/ResultRef/BoardSpec/Audience/Error 映射到真实代码名。
2. 提交经营问题 → API/service/W4 → 所需新计算 → 支持状态矩阵。未核清的新老/小样/F/会员标 UNKNOWN，不凭空填规则。
3. 冻结字段/必填/单位/枚举/可空、权限身份、实际执行条件、as_of/发布时间/规则版本。
4. 每族至少：成功、空结果、参数错误、权限失败 fixture；编辑另有 409 与部分成功。
5. Python/TS 生成类型一致；用既有离线生成器。不要为生成类型默认启动旧 CRM；不要占用 8000/5173/4327。若必须临时 OpenAPI，只用 `127.0.0.1:18080` 且结束即停。
6. 默认值集中可配置并标「待确认」：推荐「剔除小样只过滤本期销售、历史重算显式选择」和「一组认可分析一板多块、可选批量分板」——先提供显式模式，不把推荐写成隐藏假设。
7. manifest 含文件清单、hash、版本、接口支持状态。C0 产出后计算 `contract_hash`（对 contracts 目录与 manifest 的稳定 sha256）写入交付 JSON 与 `docs/hackathon/parallel-competition-2026-09-09/contracts/C0-MANIFEST.json`。

## 写入范围

只改：`backend/contracts/`、`dsh-plugins/analytics-workbench/src/*contract.generated.d.ts`、`scripts/dsh-b0/*contract.mjs`（合同相关）、`docs/hackathon/parallel-competition-2026-09-09/contracts/`、`docs/hackathon/parallel-competition-2026-09-09/evidence/A1/`。

禁止：业务计算、UI、依赖锁、`backend/main.py`、插件 `src/index.ts`/`client/index.tsx`、公共 jobs/runtime。需要公共接线则写 `integration_requests`。

## 验收对应

T01–T07 契约部分；T09/T10/T12 状态 Schema；生成器重复执行无漂移；旧 SNAPSHOT 读取兼容或明确拒绝。本批以合同/fixture/生成器为主，不宣称业务计算已修。
