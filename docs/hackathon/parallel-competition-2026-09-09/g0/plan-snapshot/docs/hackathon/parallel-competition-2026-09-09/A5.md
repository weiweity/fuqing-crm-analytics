# A5｜分析资产与看板后端

状态：PLANNED，尚未派发。建议分支名：`codex/competition-a5`；绝对工作树由Grok创建后写入派发消息，禁止发送未填写cwd的任务。

启动条件：C0后与A2/A3/A8并行。

## 任务

扩展现有资产/驾驶舱实现：认可具体结果集合、批量多板/多块、逐板原子保存、稳定block/board身份、预览与版本冲突、持久编辑目标/attempt、撤销和重开恢复。批次ID+逐板operation_id/固定payload指纹；保存版本和回执同事务，重试不重复创建。结果共享引用，不按板重复计算。

## 唯一写入范围

- `backend/services/analytics/saved_analyses.py`
- `backend/services/analytics/analysis_source.py`
- `backend/services/analytics/cockpit.py`
- `backend/services/analytics/cockpit_source.py`
- `backend/services/analytics/competition_assets/（需要时新增，先复用已有模块）`
- `backend/tests/test_competition_assets*.py（拟新增）`

## 输入与协作合同

C0 BoardSpec与结果合同；A7只提交受控patch，A6只消费渲染/状态接口；HTTP应用注册、任务账本共用层由总控接线。

## 出口产物

资产/看板服务、版本与回执持久化、403/409/失效来源/部分成功行为、旧SNAPSHOT新格式兼容矩阵、接线函数与路由补丁建议。

## 验收

T06资产权限、T09批量幂等、T10持久scope、T11保存冲突/撤销/进程重启；升级写入后降级读取与引用保留。

## 本轨额外边界

禁止实现模型聊天编排或图表UI、改公共Schema和A8人群状态；不另造独立任务账本或以会话日志代替资产。

## 共同执行约束

先读仓库 AGENTS.md、本任务、上位总计划§12及本轮测试计划；UI轨完整读 DESIGN.md。有`.codegraph/`时按仓库规则定位源码。只在Grok分配的独立工作树中工作；开始报告实际绝对cwd、分支、HEAD、dirty状态、C0版本。不要自行创建/切换分支、commit/push/PR/merge，不再派子Agent。任务文件中的路径是所有权边界，不是可以不读源码就覆盖整个目录的授权。

主线保留DSH单一运行时与全部原生功能；默认GSV，优先同期、上周同星期、大促自选。小样默认计首购/RFM，剔除路径必须显式；不得猜派样渠道、会员历史或有效订单规则。仅用生成的合成数据；不得读写/复制归档真实DuckDB、读取输出凭据、迁移/重跑真实ETL或自动公网部署/营销发送。不得关闭认证绕过问题。

每轨提交前运行与改动相关的既有验证；不新增独立测试框架。需要公共文件改动先提交接口变更申请给Grok/A1：路径、原因、最小diff建议、影响消费者、对应负测。不要因等待接线停下全部工作，继续合同fixture内的实现与局部测试。

结束提交 changed_files、base SHA、contract hash、补丁/新增文件清单、验证命令/退出码/日志、未验/风险、公共接线需求、owned进程列表。只能停止自己创建的验证实例，不动用户现有演示。COMPLETE表示本轨出口通过，不表示整产品完成。
