# A7｜电商分析Skill与DSH工具编排

状态：PLANNED，尚未派发。建议分支名：`codex/competition-a7`；绝对工作树由Grok创建后写入派发消息，禁止发送未填写cwd的任务。

启动条件：C0后可写工具/Skill与离线eval；真接线依赖A2/A3/A5/A8。

## 任务

在DSH单一Agent Loop内注册电商业务能力，编写诊断Skill：GSV→同比/周对比→渠道→小样→新老/会员→产品→RFM→固定同期cohort→未回购→草稿。实现条件继承/显式变更、支持状态识别、完整证据引用、无数据/失败/拒答处理，以及点选后受控patch生成。Skill讲方法；认证/预算/参数校验由适配与服务执行。

## 唯一写入范围

- `dsh-plugins/analytics-workbench/src/competition-agent/（拟新增）`
- `dsh-plugins/analytics-workbench/skills/competition-growth/（拟新增）`
- `backend/services/analytics/competition_diagnosis/（必要编排适配，不能重复计算）`
- `docs/hackathon/parallel-competition-2026-09-09/evidence/A7/（拟新增）`

## 输入与协作合同

C0能力目录与实际条件、A3HTTP、A5编辑目标/版本、A8人群接口。原生工具注册入口/pack-skills/lock由总控集成，交exports与接线说明。

## 出口产物

可装配的工具模块、Skill、成功/反例eval数据；调用计划与实际条件可追踪，纯样式patch不查询、筛选改动创建新run，完整分析与部分完成区别明确。

## 验收

T13按真实模型/离线stub分别报告，T03条件继承、T08预算/取消、T10非法/越界patch；prompt injection不扩工具权限，不凭GSV断言最佳投放ROI。

## 本轨额外边界

禁止改公共client/index、后端任务账本、UI组件或重复实现RFM/资产事务；不把全部旧路由直接暴露给模型，不安装第二Agent运行时。

## 共同执行约束

先读仓库 AGENTS.md、本任务、上位总计划§12及本轮测试计划；UI轨完整读 DESIGN.md。有`.codegraph/`时按仓库规则定位源码。只在Grok分配的独立工作树中工作；开始报告实际绝对cwd、分支、HEAD、dirty状态、C0版本。不要自行创建/切换分支、commit/push/PR/merge，不再派子Agent。任务文件中的路径是所有权边界，不是可以不读源码就覆盖整个目录的授权。

主线保留DSH单一运行时与全部原生功能；默认GSV，优先同期、上周同星期、大促自选。小样默认计首购/RFM，剔除路径必须显式；不得猜派样渠道、会员历史或有效订单规则。仅用生成的合成数据；不得读写/复制归档真实DuckDB、读取输出凭据、迁移/重跑真实ETL或自动公网部署/营销发送。不得关闭认证绕过问题。

每轨提交前运行与改动相关的既有验证；不新增独立测试框架。需要公共文件改动先提交接口变更申请给Grok/A1：路径、原因、最小diff建议、影响消费者、对应负测。不要因等待接线停下全部工作，继续合同fixture内的实现与局部测试。

结束提交 changed_files、base SHA、contract hash、补丁/新增文件清单、验证命令/退出码/日志、未验/风险、公共接线需求、owned进程列表。只能停止自己创建的验证实例，不动用户现有演示。COMPLETE表示本轨出口通过，不表示整产品完成。
