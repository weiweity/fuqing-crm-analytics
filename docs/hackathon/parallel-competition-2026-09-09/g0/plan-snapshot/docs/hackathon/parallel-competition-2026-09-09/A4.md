# A4｜DSH基座、品牌壳与统一主题

状态：PLANNED，尚未派发。建议分支名：`codex/competition-a4`；绝对工作树由Grok创建后写入派发消息，禁止发送未填写cwd的任务。

启动条件：第一批；不依赖业务接口修完。

## 任务

完善固定DSH启动/认证/陈旧构建诊断，冻结原生功能及品牌入口保留矩阵。依据DESIGN.md用已有Logo/字体和Ant Design tokens打造比赛品牌壳，覆盖欢迎/导航/标题/错误/弹窗；保留会话/工具/文件/权限/设置/preset/压缩可达性。新增共享theme与证据UI基础组件给A6复用。

## 唯一写入范围

- `scripts/dsh-dev/`
- `dsh-plugins/analytics-workbench/src/client/styles.ts`
- `dsh-plugins/analytics-workbench/src/client/competition-shell/（拟新增）`
- `docs/hackathon/parallel-competition-2026-09-09/evidence/A4/（拟新增）`

## 输入与协作合同

读取DESIGN.md与交互v1.3；按实际slots验证，不改固定上游。交A6的接口是ThemeProvider、LayoutSlot及公共状态/证据组件，具体export在C0协作时登记。

## 出口产物

主题/壳导出组件、原生功能PASS/FAIL/NOT_RUN矩阵、官方扩展可达性/缺口表、固定版本/许可记录、三档页面截图与合成quickstart；接入client/index由总控负责。

## 验收

T17原生功能/品牌/键盘/三档；真实模型依赖项如未配置标NOT_RUN。端口和进程由总控分配，仅管理本轨owned实例。

## 本轨额外边界

禁止改看板业务组件/AI工具/公共client入口/依赖锁；不重画品牌、不删原生能力、不关闭认证。

## 共同执行约束

先读仓库 AGENTS.md、本任务、上位总计划§12及本轮测试计划；UI轨完整读 DESIGN.md。有`.codegraph/`时按仓库规则定位源码。只在Grok分配的独立工作树中工作；开始报告实际绝对cwd、分支、HEAD、dirty状态、C0版本。不要自行创建/切换分支、commit/push/PR/merge，不再派子Agent。任务文件中的路径是所有权边界，不是可以不读源码就覆盖整个目录的授权。

主线保留DSH单一运行时与全部原生功能；默认GSV，优先同期、上周同星期、大促自选。小样默认计首购/RFM，剔除路径必须显式；不得猜派样渠道、会员历史或有效订单规则。仅用生成的合成数据；不得读写/复制归档真实DuckDB、读取输出凭据、迁移/重跑真实ETL或自动公网部署/营销发送。不得关闭认证绕过问题。

每轨提交前运行与改动相关的既有验证；不新增独立测试框架。需要公共文件改动先提交接口变更申请给Grok/A1：路径、原因、最小diff建议、影响消费者、对应负测。不要因等待接线停下全部工作，继续合同fixture内的实现与局部测试。

结束提交 changed_files、base SHA、contract hash、补丁/新增文件清单、验证命令/退出码/日志、未验/风险、公共接线需求、owned进程列表。只能停止自己创建的验证实例，不动用户现有演示。COMPLETE表示本轨出口通过，不表示整产品完成。
