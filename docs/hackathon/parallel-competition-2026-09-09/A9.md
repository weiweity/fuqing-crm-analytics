# A9｜独立验收、故障验证与容量

状态：PLANNED，尚未派发。建议分支名：`codex/competition-a9`；绝对工作树由Grok创建后写入派发消息，禁止发送未填写cwd的任务。

启动条件：第一批只设计独立fixture/验收表；集成后由总控调度实测。

## 任务

独立核对T01–T17，先建立不复制实现的手算反例与检查表。候选集成后验证诊断→认可→批量成板→点选/拖拽→保存/重开→候选→草稿；加撤权/取消迟到/409/大消息/崩溃回执/升级后降级。按固定到达负载测冷热延迟/P95/RSS和多板渲染/分页，区分演示、模型、权限、容量。

## 唯一写入范围

- `backend/tests/test_competition_acceptance*.py（拟新增）`
- `dsh-plugins/analytics-workbench/tests/competition-acceptance/（拟新增，框架按现有结构接入）`
- `docs/hackathon/parallel-competition-2026-09-09/evidence/A9/（拟新增）`

## 输入与协作合同

同一集成源码快照与C0 hash、A1–A8交付报告和已有verification.md；各轨报告不是自动通过证据，需要核对对应SHA/产物。

## 出口产物

分层PASS/FAIL/NOT_RUN矩阵、问题复现与归属、集成回归/浏览器/模型/容量证据；提交修复建议给owner，不抢改业务文件。

## 验收

全部T01–T17，性能全部到达请求计分母，真实模型缺配置明确NOT_RUN；原生完整功能和品牌显示单列结论。

## 本轨额外边界

禁止自行修业务代码/改共同fixtures或runner/测试放宽；不得把所有测试进程并发开满或读真实库。

## 共同执行约束

先读仓库 AGENTS.md、本任务、上位总计划§12及本轮测试计划；UI轨完整读 DESIGN.md。有`.codegraph/`时按仓库规则定位源码。只在Grok分配的独立工作树中工作；开始报告实际绝对cwd、分支、HEAD、dirty状态、C0版本。不要自行创建/切换分支、commit/push/PR/merge，不再派子Agent。任务文件中的路径是所有权边界，不是可以不读源码就覆盖整个目录的授权。

主线保留DSH单一运行时与全部原生功能；默认GSV，优先同期、上周同星期、大促自选。小样默认计首购/RFM，剔除路径必须显式；不得猜派样渠道、会员历史或有效订单规则。仅用生成的合成数据；不得读写/复制归档真实DuckDB、读取输出凭据、迁移/重跑真实ETL或自动公网部署/营销发送。不得关闭认证绕过问题。

每轨提交前运行与改动相关的既有验证；不新增独立测试框架。需要公共文件改动先提交接口变更申请给Grok/A1：路径、原因、最小diff建议、影响消费者、对应负测。不要因等待接线停下全部工作，继续合同fixture内的实现与局部测试。

结束提交 changed_files、base SHA、contract hash、补丁/新增文件清单、验证命令/退出码/日志、未验/风险、公共接线需求、owned进程列表。只能停止自己创建的验证实例，不动用户现有演示。COMPLETE表示本轨出口通过，不表示整产品完成。
