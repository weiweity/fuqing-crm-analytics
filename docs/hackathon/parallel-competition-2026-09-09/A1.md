# A1｜共享合同与能力目录

状态：PLANNED，尚未派发。建议分支名：`codex/competition-a1`；绝对工作树由Grok创建后写入派发消息，禁止发送未填写cwd的任务。

启动条件：第一批；先冻结C0，再持续处理变更申请。

## 任务

读取旧CRM semantic/route/service与已有analytics合同，提交经营问题→API/service/W4→所需新计算→支持状态矩阵。冻结Condition、结果引用、BoardSpec/patch、候选/草稿、错误和能力描述合同；生成Python/TS同源类型以及纯合成成功/空/错误/409例子。负责API-03中的Schema/文档漂移部分。不要另造一份绕开已有analytics合同的模型。

## 唯一写入范围

- `backend/contracts/（本主线涉及的Schema/OpenAPI产物）`
- `dsh-plugins/analytics-workbench/src/*contract.generated.d.ts`
- `scripts/dsh-b0/*contract.mjs（已有离线生成器，限合同相关）`
- `docs/hackathon/parallel-competition-2026-09-09/contracts/（拟新增）`

## 输入与协作合同

C0包含精确字段/必填/单位/枚举/可空语义、权限身份传递、实际执行条件、as_of/发布时间/规则版本；所有消费方对同一manifest hash对齐。新老/小样/F/会员未核清的语义必须标未知，不凭空填规则。

## 出口产物

版本化C0 manifest、合同变更记录、支持矩阵、生成类型、fixture及反例；消费者可离线导入并验证。C0的概念名不强制新增同名端点，按现有结构给出真实映射。

## 验收

T01–T07契约部分；T09/T10/T12状态Schema；生成器重复执行无漂移；旧SNAPSHOT读取兼容或明确拒绝。

## 本轨额外边界

禁止修改业务计算、UI实现、依赖锁或公共应用注册；消费者要改字段须向你提交CR，不能自己改generated文件。

## 共同执行约束

先读仓库 AGENTS.md、本任务、上位总计划§12及本轮测试计划；UI轨完整读 DESIGN.md。有`.codegraph/`时按仓库规则定位源码。只在Grok分配的独立工作树中工作；开始报告实际绝对cwd、分支、HEAD、dirty状态、C0版本。不要自行创建/切换分支、commit/push/PR/merge，不再派子Agent。任务文件中的路径是所有权边界，不是可以不读源码就覆盖整个目录的授权。

主线保留DSH单一运行时与全部原生功能；默认GSV，优先同期、上周同星期、大促自选。小样默认计首购/RFM，剔除路径必须显式；不得猜派样渠道、会员历史或有效订单规则。仅用生成的合成数据；不得读写/复制归档真实DuckDB、读取输出凭据、迁移/重跑真实ETL或自动公网部署/营销发送。不得关闭认证绕过问题。

每轨提交前运行与改动相关的既有验证；不新增独立测试框架。需要公共文件改动先提交接口变更申请给Grok/A1：路径、原因、最小diff建议、影响消费者、对应负测。不要因等待接线停下全部工作，继续合同fixture内的实现与局部测试。

结束提交 changed_files、base SHA、contract hash、补丁/新增文件清单、验证命令/退出码/日志、未验/风险、公共接线需求、owned进程列表。只能停止自己创建的验证实例，不动用户现有演示。COMPLETE表示本轨出口通过，不表示整产品完成。
