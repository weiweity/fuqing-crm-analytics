# A6｜可编辑看板与人群行动前端

状态：PLANNED，尚未派发。建议分支名：`codex/competition-a6`；绝对工作树由Grok创建后写入派发消息，禁止发送未填写cwd的任务。

启动条件：C0+A4主题导出后开发；A5/A8未完成时消费合同fixture。

## 任务

实现认可结果选择/确认页、一板多块与批量分板、可拖拽缩放的登记图表、点选scope聊天入口、差异预览/保存/撤销/冲突/离页恢复。实现候选规则/人数/入选解释、AND/OR组合、零人群、草稿编辑/复核/来源过期UI。沿A4主题统一，优先react-grid-layout隔离小样；visualizer仅借用点选设计，不持久任意HTML。

## 唯一写入范围

- `dsh-plugins/analytics-workbench/src/client/asset-overlay.tsx`
- `dsh-plugins/analytics-workbench/src/cockpit-view.tsx`
- `dsh-plugins/analytics-workbench/src/cockpit-model.mjs及配套.d.mts`
- `dsh-plugins/analytics-workbench/src/client/competition-board/（拟新增）`
- `dsh-plugins/analytics-workbench/src/client/competition-actions/（拟新增）`

## 输入与协作合同

C0 generated类型/fixtures，A4主题exports，A5资产接口，A8候选/草稿接口，A7编辑请求/响应；不能为等待接口自建另一套字段或本地数据权威。

## 出口产物

真实组件与编译后DOM、键盘替代/移动端单列、HTTP替身可替换的transport边界、原生mount/dispose导出；总控负责公共入口注册。

## 验收

T09–T12界面、T15用户路径、T17三档与焦点；切选中不改变在途目标；模型不可用仍可查看已存板/手动编辑。

## 本轨额外边界

禁止改全局styles/brand入口、后端Schema、AI工具/Skill或依赖锁；依赖请求由总控固定版本后统一加入。

## 共同执行约束

先读仓库 AGENTS.md、本任务、上位总计划§12及本轮测试计划；UI轨完整读 DESIGN.md。有`.codegraph/`时按仓库规则定位源码。只在Grok分配的独立工作树中工作；开始报告实际绝对cwd、分支、HEAD、dirty状态、C0版本。不要自行创建/切换分支、commit/push/PR/merge，不再派子Agent。任务文件中的路径是所有权边界，不是可以不读源码就覆盖整个目录的授权。

主线保留DSH单一运行时与全部原生功能；默认GSV，优先同期、上周同星期、大促自选。小样默认计首购/RFM，剔除路径必须显式；不得猜派样渠道、会员历史或有效订单规则。仅用生成的合成数据；不得读写/复制归档真实DuckDB、读取输出凭据、迁移/重跑真实ETL或自动公网部署/营销发送。不得关闭认证绕过问题。

每轨提交前运行与改动相关的既有验证；不新增独立测试框架。需要公共文件改动先提交接口变更申请给Grok/A1：路径、原因、最小diff建议、影响消费者、对应负测。不要因等待接线停下全部工作，继续合同fixture内的实现与局部测试。

结束提交 changed_files、base SHA、contract hash、补丁/新增文件清单、验证命令/退出码/日志、未验/风险、公共接线需求、owned进程列表。只能停止自己创建的验证实例，不动用户现有演示。COMPLETE表示本轨出口通过，不表示整产品完成。
