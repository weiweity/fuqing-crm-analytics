# A8｜固定人群追踪、召回候选与行动草稿

状态：PLANNED，尚未派发。建议分支名：`codex/competition-a8`；绝对工作树由Grok创建后写入派发消息，禁止发送未填写cwd的任务。

启动条件：C0后与A2/A3/A5并行；先做独立金标准，真实特征依赖A2。

## 任务

复用已有候选family与A2共享特征，固定去年人群再追本期：原渠道没回、原产品未复购、全店没回分别计算。支持显式AND/OR和授权域去重，保存成员规则/版本/证据，不把今年RFM重新选人冒充去年cohort。产生候选预览和行动草稿，含负责人/预算上限/复核人/复盘日期/对照/停止条件/未知项，规则变更标草稿过期。

## 唯一写入范围

- `backend/services/analytics/family_handoff_audience.py`
- `backend/services/analytics/competition_audience/（拟新增）`
- `backend/tests/test_competition_audience*.py（拟新增）`

## 输入与协作合同

C0候选/草稿合同，A2历史特征、A5结果引用；会员历史不存在则UNKNOWN，不猜；A6负责全部候选/草稿UI；HTTP注册交总控。 <!-- 收口注：已核对为原计划/条件性描述，保留原文，不作为当前实现通过声明。 -->

## 出口产物

人群计算与持久规则/草稿服务、API接线说明、授权域身份映射约束、来源时态和数据截止标记、无真实触达的完整运营闭环。

## 验收

T05固定10人反例、T06跨域/运行中撤权、T12规则变更/零人数/草稿、T14晚到退款和历史会员时态。

## 本轨额外边界

禁止重写A2共享RFM/ETL、改A5资产服务、开发另一套前端或真实发送营销消息。

## 共同执行约束

先读仓库 AGENTS.md、本任务、上位总计划§12及本轮测试计划；UI轨完整读 DESIGN.md。有`.codegraph/`时按仓库规则定位源码。只在Grok分配的独立工作树中工作；开始报告实际绝对cwd、分支、HEAD、dirty状态、C0版本。不要自行创建/切换分支、commit/push/PR/merge，不再派子Agent。任务文件中的路径是所有权边界，不是可以不读源码就覆盖整个目录的授权。

主线保留DSH单一运行时与全部原生功能；默认GSV，优先同期、上周同星期、大促自选。小样默认计首购/RFM，剔除路径必须显式；不得猜派样渠道、会员历史或有效订单规则。仅用生成的合成数据；不得读写/复制归档真实DuckDB、读取输出凭据、迁移/重跑真实ETL或自动公网部署/营销发送。不得关闭认证绕过问题。

每轨提交前运行与改动相关的既有验证；不新增独立测试框架。需要公共文件改动先提交接口变更申请给Grok/A1：路径、原因、最小diff建议、影响消费者、对应负测。不要因等待接线停下全部工作，继续合同fixture内的实现与局部测试。

结束提交 changed_files、base SHA、contract hash、补丁/新增文件清单、验证命令/退出码/日志、未验/风险、公共接线需求、owned进程列表。只能停止自己创建的验证实例，不动用户现有演示。COMPLETE表示本轨出口通过，不表示整产品完成。
