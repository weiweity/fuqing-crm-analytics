# A2｜旧CRM计算与日期口径

状态：PLANNED，尚未派发。建议分支名：`codex/competition-a2`；绝对工作树由Grok创建后写入派发消息，禁止发送未填写cwd的任务。

启动条件：第一批可只读盘查/设计手算夹具；C0后实现。

## 任务

完成API-01/02计算部分：月初MTD/闰日/同比/上周同星期/自选双窗，GSV、新老按分析start前历史、订单粒度F、R/as_of、销售与历史scope分离、小样按派样渠道两条排除路径、会员/未知。先追CTE粒度，不能直接把COUNT(*)判错后盲改。复用现有W4/W5特征，不重构整仓ETL。

## 唯一写入范围

- `backend/services/metrics/audience_summary.py及相关人群计算文件`
- `backend/services/rfm/`
- `backend/semantic/（若实际存在且属于本轨口径；先核实）`
- `backend/services/analytics/customer_features/（仅必要共享特征适配）`
- `backend/tests/test_competition_metrics*.py（拟新增）`

## 输入与协作合同

C0与旧口径源码；输出实际执行条件及支持范围，不能接受筛选却忽略。历史cohort所需特征接口与A8对齐；A8只能调用，不能重复实现RFM。

## 出口产物

可调用的共享计算入口、独立手算金标准、历史默认兼容说明、API需暴露的参数清单；route改动交A3，Schema交A1。

## 验收

T01–T05计算部分和T14日期推进/历史版本；覆盖一订单多明细、月中首购、跨渠道回购、未知会员、退款及样本排除。

## 本轨额外边界

禁止改路由、contracts、MCP、UI和真实ETL/数据；若派样/会员历史来源不够，明确不支持并向总控报事实缺口。

## 共同执行约束

先读仓库 AGENTS.md、本任务、上位总计划§12及本轮测试计划；UI轨完整读 DESIGN.md。有`.codegraph/`时按仓库规则定位源码。只在Grok分配的独立工作树中工作；开始报告实际绝对cwd、分支、HEAD、dirty状态、C0版本。不要自行创建/切换分支、commit/push/PR/merge，不再派子Agent。任务文件中的路径是所有权边界，不是可以不读源码就覆盖整个目录的授权。

主线保留DSH单一运行时与全部原生功能；默认GSV，优先同期、上周同星期、大促自选。小样默认计首购/RFM，剔除路径必须显式；不得猜派样渠道、会员历史或有效订单规则。仅用生成的合成数据；不得读写/复制归档真实DuckDB、读取输出凭据、迁移/重跑真实ETL或自动公网部署/营销发送。不得关闭认证绕过问题。

每轨提交前运行与改动相关的既有验证；不新增独立测试框架。需要公共文件改动先提交接口变更申请给Grok/A1：路径、原因、最小diff建议、影响消费者、对应负测。不要因等待接线停下全部工作，继续合同fixture内的实现与局部测试。

结束提交 changed_files、base SHA、contract hash、补丁/新增文件清单、验证命令/退出码/日志、未验/风险、公共接线需求、owned进程列表。只能停止自己创建的验证实例，不动用户现有演示。COMPLETE表示本轨出口通过，不表示整产品完成。
