# A3｜API治理、读取预算与旧MCP修复

状态：PLANNED，尚未派发。建议分支名：`codex/competition-a3`；绝对工作树由Grok创建后写入派发消息，禁止发送未填写cwd的任务。

启动条件：C0后与A2/A5/A8并行；计算未交付时仅用C0注入夹具。

## 任务

完成API-03认证/错误接入与能力目录实际调用映射、API-04精确只读POST准入、API-05完整输出/合法JSON/超时取消、API-06请求预算与基准所需观测接口。优先HTTP适配；旧MCP问题必须交代但不阻断无关HTTP直连。补curl/Python正负例、分页/结果引用与权限检查。

## 唯一写入范围

- `backend/routers/audience.py及本主线需暴露参数的只读路由`
- `backend/middleware/query_router.py`
- `backend/services/dual_conn.py`
- `backend/db/connection.py`
- `mcp_servers/fuqing_adhoc/`
- `docs/development/services.md`
- `backend/tests/test_competition_api*.py（拟新增）`

## 输入与协作合同

C0错误/条件合同，A2计算签名。backend/main.py、任务账本与应用接线由总控集成，你提供小范围接线补丁建议，不越权改公共入口。

## 出口产物

可信只读HTTP调用、明确401/403/422/429/503、完整结果读取方式、受控连接准入、API文档与MCP修复证据；结构化性能事件，不宣称容量通过。

## 验收

T03筛选执行、T06权限、T07大消息/截断、T08池满取消恢复；请求结束不得关闭他人连接；验证旧MCP如保留不再串行无限阻塞。

## 本轨额外边界

禁止改A2计算、A1合同/生成类型、全部ad-hoc一概归只读、无界重试或盲目扩大连接池。

## 共同执行约束

先读仓库 AGENTS.md、本任务、上位总计划§12及本轮测试计划；UI轨完整读 DESIGN.md。有`.codegraph/`时按仓库规则定位源码。只在Grok分配的独立工作树中工作；开始报告实际绝对cwd、分支、HEAD、dirty状态、C0版本。不要自行创建/切换分支、commit/push/PR/merge，不再派子Agent。任务文件中的路径是所有权边界，不是可以不读源码就覆盖整个目录的授权。

主线保留DSH单一运行时与全部原生功能；默认GSV，优先同期、上周同星期、大促自选。小样默认计首购/RFM，剔除路径必须显式；不得猜派样渠道、会员历史或有效订单规则。仅用生成的合成数据；不得读写/复制归档真实DuckDB、读取输出凭据、迁移/重跑真实ETL或自动公网部署/营销发送。不得关闭认证绕过问题。

每轨提交前运行与改动相关的既有验证；不新增独立测试框架。需要公共文件改动先提交接口变更申请给Grok/A1：路径、原因、最小diff建议、影响消费者、对应负测。不要因等待接线停下全部工作，继续合同fixture内的实现与局部测试。

结束提交 changed_files、base SHA、contract hash、补丁/新增文件清单、验证命令/退出码/日志、未验/风险、公共接线需求、owned进程列表。只能停止自己创建的验证实例，不动用户现有演示。COMPLETE表示本轨出口通过，不表示整产品完成。
