# S4 综合 QA（2026-09-15）

固定候选：origin/main `775934f5`，DSH `http://127.0.0.1:6677/` PID 12386，插件来自 `fuqing-crm-analytics-main-runtime/dsh-plugins/analytics-workbench`。未 `--fresh`，未改说明板 v15／原板 v9。产品仍 **PARTIAL**。不勾 G1–G6，不代签 U1。

本机账本 `.context/checks/s4-g1-g5-20260915/`（不进 Git）。

## 候选钉死

| 项 | 值 |
|---|---|
| Git | origin/main `775934f5`（#160）；VERSION 仍 0.8.0.0 |
| DSH 钉 | 0.1.5-rc.1 `183f08e9` |
| 6677 | web PID 12386，supervisor 12373，runtime `.context/dsh-dev/runtime` |
| 插件 src | 与 origin/main 相同 |
| 插件构建 | 6677 用 main-runtime `lib/client.js` 短哈希 `08616945adc0562e`。S4 取证时原仓 lib 为旧包 `c93eb74f86398b59`；本轮 B0 `--check` 后原仓工作树 lib 已建成同一短哈希。6677 未 reload，仍以启动时加载的 main-runtime 构建为准 |
| 18082 | PID 71160，origin 允许 6677，状态 `.context/checks/s2-c1/synth-state` |
| 比赛看板 | `http://127.0.0.1:15173/` + API 8000，均在 `main-runtime`（独立探活，不是 T15 运营路径） |

9 月 14 日 S2 真模型是另一组 6677 PID／原仓插件，只作历史，不能直接勾本候选。

## G1–G5

| 门槛 | 结论 | 本轮做了什么 | 仍缺 |
|---|---|---|---|
| G1 真实 AI | **NOT_RUN** | 无新模型调用 | 本候选上六类自主编辑与自然语言组板 |
| G2 数据与范围 | **PARTIAL** | 隔离 backend 120；board-spec 151；board-tools 6；加载插件 HTTP 集成通过（TABLE `show_values` 预检为 `COMPONENT_PROPERTY`，重叠预检保留 FUNNEL） | 现场 409／撤权／会话串扰 |
| G3 保存恢复 | **PARTIAL** | 隔离确认／取消／幂等／409／回退／重开；只读 GET 仍为 v15／v9，0 次模型 | 现场确认保存 |
| G4 原生体验 | **PARTIAL** | Chrome 桌面壳层；composition 键盘调宽／窄屏覆盖／草稿身份；lifecycle 底栏「比赛看板」新标签 | 现场折叠调宽键盘、插件插拔热更 |
| G5 设计可用性 | **PARTIAL** | 390 视口截图；compact navigation DOM | 完整 Figma 点击、长内容、性能；不重跑 B3 |

85 历史跳转仍未定因。合成测试不能代替本人 UAT。

## 保留的失败

对**原仓旧构建** `lib/client.js` 跑同一套生命周期／HTTP 集成会失败（少一个 footer、R-1 预检码变成 `INVALID_REQUEST`）。当前 6677 不加载该文件。G6 写明：不要用原仓 `lib/` 当运行证据。

## 接续

S5 入口与回退见 [S5 交接](S5-UAT-HANDOVER-2026-09-15.md)。G1 若要补，须另开**新板**并明确授权模型预算，不要在 v15／v9 上确认保存。
