# 阶段草稿提交检查

日期：2026-09-06。阶段：详细设计尚未全部收口，B0 局部实现与合成验证已完成，整体 **B0 PARTIAL**。本文是 commit 前的检查快照，不代表远端 CI、合并或发布结果。

## 本次验证

- 统一 B0 `pipeline.mjs --check`：164 Python、133 源 Node、原目录及干净目录各 14 编译组件/装配测试通过，完整类型、loader 和构建一致性通过。14 项含 9 项组件回归，不全部称为原生装配。
- 旧 Vue：23 个测试文件、201 项单元测试通过；vue-tsc / Vite 构建通过，保留大于 500 kB 的 chunk 警告。
- 追加资源默认值/连接、派样首值聚合、Agent 规则入口与 hook 回归：71 PASS（85.98 秒）；不替代完整旧 CRM 测试。
- 主 Agent 后端重点检查与同模型独立前端、插件/流水线、计划/公开资产审查。未重复原生七问、浏览器视觉验收或真实模型验证；已有历史运行证据仍按各报告的时间与层级引用。
- 当前总览同步至 T08，修正 ETL 诊断源码链接格式；历史 T01–T07 的 5 项装配成绩保持不变。
- 现有 pre-commit 退出成功但输出历史测试顺序、全仓百分比/导出 lint 及文档措辞警告；部分检查经 `tail` 管道不能据总退出码证明每个子检查成功。本次没有绕过 hook，也不将这些输出记成全仓 lint 通过。
- 公开内容检查覆盖候选源码/文档、未合并提交补丁及新增二进制资产来源/图像；不纳入 `.context`、凭据、数据库或上游安装目录。扫描命中的负测 basic-auth URL、动态 token 变量、测试 ID、图形坐标、时间戳和保留示例 IP 不作为真实秘密；未发现实际凭据或客户身份记录。

## 合并前待办

1. **P2：问数响应跨越演示重置。** `GrowthBoardView.vue` 重置完成后，旧请求仍可能写回 diagnosis/error。需以请求代次隔离旧结果，并覆盖延迟成功与失败两条回归。本次仅留存，未修复。
2. **P2：登录页数据范围声明不准确。** `LoginView.vue` 无条件声明整个环境为合成数据，但共用正常 CRM 登录；应限定声明范围，不改认证权限。本次未修复。
3. 原生故障事件至工具卡全链、其他 key 卡片共存、历史监督器退出根因、完整产品/多人容量/业务验收继续按 [B0 清单](./B0-EXECUTION-CHECKLIST-2026-09-06.md) 与 [总待办](./PLAN-CLOSEOUT-2026-09-05.md) 开放，不能用当前 PASS 关闭。
4. 远端 CI、required checks、最终 merge 前 QA 尚未通过本次验证；Draft 不是合并许可。

## 项目状态为何容易读错

项目文件、B0 执行清单和 T08 证据仍在。问题是早期“当前摘要”未同步后续局部结果，并非本轮发现文件丢失。读取顺序为 AGENTS → hackathon README → 执行清单/总待办 → 对应日期证据；历史报告不替代当前状态。未写 Codex 全局记忆。

## 本次 Git 边界

用户授权按逻辑单元 commit、push、建立阶段性 Draft PR；未授权 merge、部署、公开运行、ETL、真实库读写、付费模型或对外消息。现有 hooks 不默认绕过；结果以实际 Git/CI 返回为准，不提前在本文声明完成。该分支含已有未合并提交，Draft 是阶段留存，后续功能应回归小 PR。
