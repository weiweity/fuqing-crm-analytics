# S3 原型与正式壳续验（2026-09-15）

> 最新接续见 [S3 交叉补验](S3-CROSS-MATRIX-2026-09-15.md)：核心分支已补齐两种视口证据，12 次恢复按钮忙态实点无额外请求；85 桌面取消处理中意外返回对话仍未定位。后续 88 复测阻断未生效，实际新增同正文 v15，记为 SETUP_FAILED，未复验到取消忙态。已清理临时设置并只读刷新确认 v15 正文完整、原板 v9，整体仍 PARTIAL。下文保留上一批截至 v9 的历史记录。

结论：指定 Figma 点击矩阵通过；正式 DSH 壳本轮仍为 **PARTIAL**。真实 Chrome 已覆盖 B3 九类状态的代表性路径、桌面与 390px 展示，并验证写入前中断、落盘后丢回执、取消被拒、同幂等键重试和确认后刷新失败。完整分支×视口矩阵尚未穷尽；窄屏展开侧栏挤压正文、取消拒绝文案仍有问题。说明板最终为 **v9**，追加正文完整；临时网络和视口设置已清除。G1–G6、M1、用户本人 UAT 均未勾选。

## 原型结果

在已登录的本机真实 Chrome 中，以 Computer Use 原生鼠标完成独立入口 `196:3220` 的 11 个入口、用户指定 B3 保存恢复分支、正式入口 `89:1148` 的生成取消/确认、卡片 AI 修改保存与回退。两个处理中页面共 8 个灰色按钮均已实点，无额外动作，随后返回合同指定结果。

长内容 `199:3370` 曾出现链接覆盖图表。用户明确授权修复后，仅把容器 `199:3492` 的纵向尺寸由 FIXED 改为 HUG；正文、趋势、对比、证据、表格和底部链接已逐段真实滚动复验通过。文字、节点标识、可见性和连线前后比对一致。金额仍是原型示例。

完整点击矩阵、历史失败、修复和截图已从临时目录归档至本机 `.context/checks/figma-proto-audit-20260915/REPORT.md` 与 `REPAIR.md`。该私有目录不进 Git，不是公共可访问附件。

## 正式壳本轮证据

本轮从 `main` / `9a0b3f1` 开始；原有未跟踪文件 `HANDOVER-CODEX.md` 保留。运行实例仍为 9 月 14 日的 6677（Node PID 71492）与合成 HTTP 18082（Python PID 71160），未启动或重启服务、未切模型、未新增模型调用、未读取真实 DuckDB、未修改代码或提交 Git。

验收对象为 `board_6e9b59dfe3e845328ac0fcf0cd8d1acc`「S2-C1 合成数据说明板」，起点 v2。正式壳地址始终为 `http://127.0.0.1:6677/`，无 Figma node-id；按看板版本和可见正文记录。

| 步骤 | 点击文案 | 点击前 | 点击后 | 结论 | 本机截图 |
|---|---|---|---|---|---|
| 1 | 驾驶舱 → 说明板 | 对话 | 已保存 v2 | 可打开；正文含 V-A4 追加句 | `01-desktop-saved-v2.png` |
| 2 | 查看历史版本 → 预览回退到 v1 | 已保存 v2 | 待确认 v3 草稿 | 正常预览通过；下拉仍 v2，草稿正文不含追加句 | `02-desktop-rollback-preview.png` |
| 3 | 取消，保留已保存版本 | 待确认 v3 草稿 | 已保存 v2 | 正常取消通过；追加正文恢复 | `03-desktop-cancel-v2.png` |
| 4 | 刷新已保存看板 | 已保存 v2 | 已保存 v2 | 页面回读仍 v2 | 原生 AX 回读 |
| 5 | 再预览 v1 → 确认保存 | 已保存 v2 / 待确认 v3 | 已保存 v3 | **注错未命中**，实际完成正常回退；不能计为写入前中断通过 | `05-block-not-hit-saved-v3.jpg` |
| 6 | 预览回退到 v2 | 已保存 v3 | 待确认 v4 | 恢复预览包含验收前的追加正文 | `06-restore-preview-v4.jpg` |
| 7 | 确认保存这份看板 | 待确认 v4 | 已保存 v4 | 原生 AX 回执“已保存并读取当前版本”，且包含追加正文；最终可见截图/重开复验受工具白屏阻塞 | 工具回读；勿把 `07-restored-v4.jpg` 当成功截图 |

截图及 AX 文件在本机 `.context/checks/s3-b3-shell-20260915/`。`04-desktop-confirm-blocked.png` 只记录点击尝试，未命中阻断；`07-restored-v4.jpg` 在状态完成前捕获，仍是预览，保留为过程证据，不据文件名判通过。实际恢复到 v4 的回执出现在随后关闭 DevTools 的原生 AX 回读中。v3/v4 为本轮新增回退历史，未删除版本；恢复的是正文内容，不是把版本号改回 v2。

## 早先停点（历史，不是当前阻塞）

尝试使用 Chrome Request conditions 阻断 `/shine-mage-board/confirm`，面板计数始终 `0 affected`。实际正式壳走共享原生入口 `/api/shine-mage-board`，操作位于请求体；逻辑 channel 不能当作 HTTP URL。发现未命中后停止该办法，关闭阻断总开关与 DevTools，并通过历史回退恢复正文。规则删除未取得成功证据，可能仍作为禁用项保留；后续先清理本次唯一规则。未启用正确入口的新阻断。

随后 Computer Use 出现 `elementHasNoFrame`、`noWindowsAvailable`、画面持续白屏及 AX 与截图不同步。重新绑定 Chrome、Raise、重建工具连接后仍未恢复可信可见操作。`08-computer-use-white-screen.jpg` 保留该阻塞，不能据 AX 中存在控件就推断视觉通过。

仓库已有 `test/helpers/board-fault-proxy.mjs`，支持一次性 `confirm-before`、`confirm-after`、`get`、`list` 故障；当前 6677 未挂载该代理。本轮未启动隔离服务、未切换现有服务配置，也没有用组件测试替代正式壳鼠标验收。

下表是控制恢复前的停点，不是终态；恢复后续验与九类状态代表覆盖见下一节。

| B3 合同场景（原型参考） | 正式壳桌面本轮 | 390px 本轮 |
|---|---|---|
| 写入前中断确认前 `326:8245` | NOT_RUN：仅正常预览/取消已验，故障未武装成功 | NOT_RUN |
| 写入前中断结果未知 `326:7965` | NOT_RUN | NOT_RUN |
| 只读 PENDING 仍未知 `326:8525` | NOT_RUN | NOT_RUN |
| 落盘后断连确认前 `326:8805` | NOT_RUN | NOT_RUN |
| 落盘后断连结果未知 `326:9085` | NOT_RUN | NOT_RUN |
| 取消被拒 `326:9645` | NOT_RUN | NOT_RUN |
| 已确认保存但刷新失败 `326:9365` | NOT_RUN | NOT_RUN |
| 核对处理中 `330:8896` | NOT_RUN：禁用与耗时未实测 | NOT_RUN |
| 取消处理中 `330:9177` | NOT_RUN：禁用与耗时未实测 | NOT_RUN |

9 月 14 日的 390 单列、历史焦点和 LINE 保存链仍是历史分项证据，本轮没有重验，不能合并声称九态通过。

## 浏览器控制恢复后的实际续验

用户明确同意连续操作 Chrome 后，使用已连接的真实 Chrome 标签页 `199365531`，没有创建无头会话。截图与操作均来自该标签页的 Computer Use；仅故障注入和视口覆盖使用该标签页的 DevTools 能力。正式壳地址仍是 `http://127.0.0.1:6677/`，表中版本是运行状态，不是 Figma node-id。

先真实重开说明板 v4，确认 V-A4 追加正文完整；旧 Request conditions 规则为空（09、10）。再对实际 `/api/shine-mage-board` 做只读刷新阻断，页面显示 `Failed to fetch`，浏览器事件为 `blockedReason: inspector`，v4 正文仍在（16）。验证入口命中后才执行保存故障。

落盘后故障使用限定业务 Fetch URL 的 **Response 阶段**拦截：仅在 `operation=confirm` 且实际 HTTP 200 时丢弃回执。写入前精确故障使用 **Request 阶段**拦截确认。回执丢失后通过真实按钮核对／重试／取消及重新读取版本证明结果；未启动代理、未改服务配置。审计只保存操作名、状态码、是否丢弃、幂等键是否一致，不保存请求头、凭据或键值。

以下截图均位于本机 `.context/checks/s3-b3-shell-20260915/`；同编号 `.ax.txt`、`.audit.json` 或 `.events.json` 是辅助证据。表中“通过”只覆盖该行操作。

| 步骤 | 点击文案／视口 | 点击前版本或状态 | 点击后版本或状态 | 是否符合合同 | 截图文件 |
|---|---|---|---|---|---|
| 8 | 切换 390×844；收起侧边栏 | v4，侧栏展开 | v4，单列正文与卡片可读 | 收起后通过；展开侧栏挤压正文 | `11-390-sidebar-expanded.png`、`12-390-sidebar-collapsed.png` |
| 9 | 画布内部滚动；外层滚动 | 说明正文 | 指标卡片、调整布局和历史入口 | 通过，内部滚动与外层滚动分别可用 | `13-390-bottom.png` |
| 10 | 预览回退到 v1 → 取消，保留已保存版本／390 | 已存 v4 | 草稿 v5 → 已存 v4，追加句恢复 | 通过 | `14-390-rollback-preview.png`、`15-390-cancel-v4.png` |
| 11 | 刷新已保存看板／桌面，阻断实际入口 | 已存 v4 | 刷新失败，旧快照保留 | 通过，真实命中阻断 | `16-blocked-read.png` |
| 12 | 确认保存这份看板／桌面，写入前阻断 | 已存 v4／草稿 v5 | 保存结果待核对 | 通过 | `17-before-write-confirm.png`、`18-before-write-unknown.png` |
| 13 | 核对保存结果／桌面 | 结果未知 | 服务端暂未记录应用，仍未知 | 通过 | `19-readonly-pending.png` |
| 14 | 再次核对／390 | PENDING 仍未知 | 按钮禁用 → 返回仍未知 | 通过；2 秒人工延迟仅用于观察忙态 | `20-390-pending.png`、`21-390-inspect-busy.png`、`22-390-inspect-returned-pending.png` |
| 15 | 等待时实点灰色核对按钮／390 | 核对处理中 | 未产生第二个业务响应 | 分项通过；完整三按钮组合未穷尽 | `24-inspect-disabled-full.audit.json` |
| 16 | 尝试取消未应用草稿／390 | PENDING 草稿 v5 | 处理中 → 取消回执 → 已存 v4，追加句完整 | 通过 | `25-390-cancel-busy.png`、`26-390-pending-canceled-v4.png` |
| 17 | 预览回退到 v2 → 确认／桌面，丢弃成功回执 | 已存 v4／同正文草稿 v5 | 服务端确认 200；前端仍未知 | 通过 | `27-after-write-before-confirm.png`、`28-after-write-unknown.png` |
| 18 | 尝试取消未应用草稿／桌面 | 已落盘但前端未知 | 取消被拒，正文保留 | 行为通过；拒绝文案有歧义 | `29-applied-cancel-rejected.png` |
| 19 | 再取消／390 | 取消被拒 | 禁用处理中 → 再次被拒 | 通过 | `30-390-cancel-rejected.png`、`31-390-rejected-cancel-busy.png`、`32-390-rejected-again.png` |
| 20 | 核对保存结果／390 | 取消被拒 | 已核对已存 v5，追加句完整 | 通过，未撤销落盘版本 | `33-390-verified-saved-v5.png` |
| 21 | 同正文回退预览 → 确认／桌面，丢弃成功回执 | 已存 v5／草稿 v6 | 服务端确认 200；前端未知 | 通过 | `34-v6-receipt-lost.png` |
| 22 | 重试这次保存／桌面 | 已落盘 v6、前端未知 | 同幂等键确认 200；随后只读 get 被注入失败 | 通过，版本仍 v6 | `35-confirmed-v6-refresh-failed.png`、`35-retry-refresh-failure.audit.json` |
| 23 | 观察确认后刷新失败 → 刷新已保存看板／390 | 已确认 v6、刷新失败 | 保留 v6 正文 → 正常回读 v6 | 通过 | `36-390-confirmed-refresh-failed.png`、`37-390-refreshed-v6.png` |
| 24 | 确认保存这份看板／390，请求发出前中断 | 已存 v6／同正文草稿 v7 | 保存结果待核对 | 通过，确认请求被阻止发送 | `38-390-v7-before-confirm.png`、`39-390-before-write-unknown.png` |
| 25 | 核对保存结果 → 重试这次保存／390 | 未写入草稿 v7 | PENDING → 同幂等键确认 200 → 已存 v7 | 通过；测试暂挂的 get 释放后回读完成 | `40-390-v7-pending.png`、`41-390-retried-saved-v7.png`、`43-390-v7-read-completed.png` |
| 26 | 未写入故障后核对、取消／桌面 | 已存 v7／未知草稿 v8 | 核对忙态 → PENDING → 取消忙态 → 已存 v7 | 通过；该草稿未保存 | `44-desktop-inspect-busy.png`、`45-desktop-cancel-busy.png`、`46-desktop-canceled-v7.png` |
| 27 | 同正文回退预览 → 确认／390，丢弃成功回执 | 已存 v7／新草稿 v8 | 服务端确认 200；前端未知 | 通过 | `47-390-after-write-before-confirm.png`、`48-390-after-write-unknown.png` |
| 28 | 核对保存结果／390 | 落盘后未知 | 直接核对已存 v8，追加句完整 | 通过 | `49-390-direct-inspect-v8.png` |
| 29 | 清理临时网络／视口设置 → 正常刷新 → 恢复侧栏 | 已存 v8 | 已存 v8，正文完整 | 通过，原板仍列为 v9 | `50-final-clean-v8.png`、`51-restored-ui-v8.png` |
| 30 | 写入前未知 → 直接取消，未经核对／桌面 | 已存 v8／未知草稿 v9 | 取消回执 → 已存 v8，正文完整 | 通过 | `52-direct-cancel-unknown.png`、`53-direct-canceled-v8.png` |
| 31 | 写入前未知 → 直接重试，未经核对／390 | 已存 v8／新草稿 v9 | 确认 200 → 已存 v9；随后 get 被注入失败 | 保存分支通过，已确认快照保留 | `54-390-direct-retry-unknown.png`、`55-390-direct-retry-saved-refresh-failed.png` |
| 32 | 确认后刷新失败 → 历史 → 预览回退到 v1 → 取消／390 | 已确认 v9、刷新失败 | 草稿 v10 尚未保存 → 取消后仍为 v9，追加句完整 | 通过，未确认回退，没有保存 v10 | `56-390-refresh-failed-rollback-preview.png`、`57-390-fallback-canceled-v9.png` |
| 33 | 清理临时设置 → 正常刷新 → 恢复侧栏 | 已存 v9 | 正常回读 v9，正文完整 | 通过，原板仍列为 v9 | `58-final-clean-v9.png`、`59-restored-ui-v9.png` |

### 九类状态的证据覆盖

这是代表性状态覆盖，不等于每个分支都分别在两种视口重跑。

| 合同参考状态 | 桌面证据 | 390px 证据 |
|---|---|---|
| 写入前确认 `326:8245` | 17 | 38 |
| 写入前结果未知 `326:7965` | 18 | 39 |
| 只读 PENDING 仍未知 `326:8525` | 19 | 20、22、40 |
| 落盘后断连确认前 `326:8805` | 27 | 47 |
| 落盘后结果未知 `326:9085` | 28、34 | 48 |
| 取消被拒 `326:9645` | 29 | 30、32 |
| 已确认但刷新失败 `326:9365` | 35 | 36 |
| 核对处理中 `330:8896` | 44 | 21 |
| 取消处理中 `330:9177` | 45（PENDING 取消） | 31（已应用拒绝后的再取消） |

Figma 的约 0.8 秒是原型合同，不能当作正式 HTTP 耗时要求。正式壳观察施加了 2000ms 延迟；完整状态审计显示核对按钮约 2379ms 后重新启用，取消约 2379ms 后结束，均保留预期结果。`23-inspect-disabled.audit.json` 使用差分树判定，属于无效测量；`24` 的 `returnedPending:false` 捕获在按钮重新启用、提示文字尚未更新的瞬间，不能解读为状态失败；之后页面回读明确返回 PENDING。本轮只对一个灰色核对按钮记录了实点无重复响应证据，没有把其它按钮的禁用外观冒充全部实点。

`41`、`42` 截图仍是确认后的刷新等待，原因是测试拦截窗口内到达的 get 响应暂挂，清空拦截模式不会自动释放它；已明确释放该已到达的只读响应，`43` 显示“已保存并读取当前版本”。这段人工暂挂不计产品处理时长通过。

### 副作用与清理

本次恢复控制后，正常取消没有新增保存版本；落盘与重试验收有意新增说明板 **v5、v6、v7、v8、v9**，各次保存预览均包含与 v4 相同的正文和 V-A4 追加句。v6 回执丢失后的重试仍为 v6，没有生成重复版本。v10 只出现于回退预览，随后取消，未保存。先前误操作留下的 v3/v4 历史继续保留。原板不参与本次保存，列表仍为 v9。

已清空 Fetch 拦截模式和 URL 阻断，释放暂挂响应，网络延迟恢复为 0，视口覆盖 reset，侧栏恢复展开，并通过正常刷新核对最终 v9。未重启 6677/18082，未改代码、模型、Git 提交或真实业务数据。临时故障能力均留在当前真实标签页，没有安装或启动代理服务。

环境观察：进入 Chrome 时已有 DSH Plugin Hub 更新进行中，非本轮 Agent 发起；完成后选择“稍后重启”。早先设备工具操作被同时操作打断，不计为 390 通过；后来使用浏览器视口能力实测的 390×844 才计证据。

## 开放项与下一步

- **失败／偏差**：390px 侧栏展开时主内容只剩很窄区域，标题和按钮逐字折行，影响可读性（11）；手动收起后恢复可用（12）。取消已应用草稿时提示“看板版本已变化……原版本未被覆盖”，没有明确说明“已保存，不能取消”（29、32）；行为拒绝正确，提示仍需改进。一般网络失败仍直接显示英文 `Failed to fetch`。
- **尚未逐项点到**：每条边在桌面与 390px 分别重跑；每个处理中按钮逐一实点及完整读／写／取消次数审计。直接取消、直接重试、刷新失败后直接进入回退预览已由步骤 30–32 补测，不能继续记作未运行；仍不能把代表性分支覆盖宣称为完整交叉矩阵通过。
- **接续**：先处理上述窄屏和文案偏差，再补剩余分支与禁用按钮矩阵；涉及代码修复按具体范围执行，不修改 DSH 上游。R-1 422/bash、完整六类视觉、S4 综合 QA、S5 用户本人 UAT 和 G1–G6 仍开放。
