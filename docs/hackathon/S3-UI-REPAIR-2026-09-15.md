# S3 窄屏与提示修复（2026-09-15）

本地修复已完成，B0 完整检查与干净重建通过；正式 Chrome 定点复验保持说明板 v15、原板 v9，本轮没有新增保存版本。产品仍 **PARTIAL**：85 历史跳转未定因，本轮未复现；取消拒绝新文案已有状态回归，未再次制造正式壳落盘故障。S4 综合 QA、S5 本人 UAT、R-1 与 G1–G6 不据此关闭。

修复取证阶段按用户授权的缺陷顺序执行，修改仅在业务插件和文档中；未改 Figma、DSH 上游、保存协议或业务数据，没有服务重启或模型调用。取证时工作位置为 `codex/docs-s3-cross-matrix-v14` / `e633d33`，代码当时未提交；该文档基线已由 #151 合入 main。用户后续明确授权本批提交、推送、建 PR 及检查通过后合并。交付使用从 main `ba13c52` 建立的独立 `codex/fix-s3-compact-navigation` 工作树，保留原目录成果和 `HANDOVER-CODEX.md`；不部署、不清理。

## 修复内容

| 问题 | 现在的行为 | 验证层级 |
|---|---|---|
| 390px 展开侧栏把正文挤入约 110px 区域 | 驾驶舱窄屏导航覆盖画布，画布保持 334px；导航开启时背景画布和聊天不可交互，实点外侧可关闭导航恢复单列 | 编译后 DOM、原生身份与卸载恢复回归；真实 Chrome 展开／关闭截图和 DOM 尺寸 |
| 取消已应用草稿显示通用“版本已变化／原版本未被覆盖” | 仅对 cancel 的 VERSION_CONFLICT 显示“草稿已保存，不能通过取消撤销”，引导核对结果和预览回退；不清除待核对草稿 | 状态回归：连续取消被拒、旧快照和未知态保留；其他操作的冲突提示不被误改。未重新制造真实落盘故障 |
| 浏览器连接异常直接显示 Failed to fetch | 读取／取消等操作提示连接中断并保留当前内容；确认保存丢回执明确提示结果待核对，不推断未保存 | 状态回归、正式 Chrome 的读取故障及写入前确认阻断 |

窄屏适配沿用已固定 DSH `183f08e9c6dde7e36cd2318eaee70b0da08fb35e`。官方 `layout.toggleSidebar()` 继续拥有开关状态；业务插件只在驾驶舱打开且 frame ≤616px 时租用样式，保留原侧栏、聊天 DOM 和布局偏好。依赖固定壳的 `data-sidebar-collapsed`、inline grid 宽度、56px rail 与既有 main/conversation 包装。关闭或卸载时释放标记和 CSS 变量。上游提供可用的窄屏抽屉接口后移除此租用；升级前须复验展开、关闭、resize、焦点、原生聊天与卸载，不散改上游源码。

## 验证结果

执行现有入口：

```sh
/Users/hutou/homebrew/opt/node@24/bin/node scripts/dsh-b0/pipeline.mjs --check --python /Users/hutou/homebrew/bin/python3.14
```

退出码 0。覆盖离线合同、486 个 Python 测试、Ruff、Node 源测试、Host/Client 类型检查、编译后 DOM、实际 Cordis 装配与干净重建；并非 S4 全产品或本人 UAT。完整日志在本机 `.context/checks/s3-ui-repair-20260915/pipeline.log`，构建证据为 `.context/dsh-b0/build-evidence.json`，本次另保存一份到修复证据目录。初次局部回归发现 React 18 不接受 boolean inert，已改为存在／缺席属性，最终编译后回归通过；样式恢复验证比较 CSS 语义及原 DOM 身份。

以下路径相对于本机 `.context/checks/s3-ui-repair-20260915/`。正式壳无 Figma node-id，以可见状态与版本记录。

| 步骤 | 点击文案／动作 | 前 → 后 | 结果 | 截图路径 |
|---|---|---|---|---|
| 01–02 | 刷新已保存看板；Request 阶段拦截 list，解除后刷新 | v15 → 读取失败 → v15 | 预检通过；实际观察到暂停事件，恢复 list/get 各一次 | `01-readonly-preflight-intercepted.png`、`02-readonly-preflight-restored.png` |
| 03 | 页面重载新构建，打开说明板，390px | 已存 v15 → 单列 v15 | 通过 | `03-390-collapsed.png` |
| 04 | 打开侧边栏 | 单列 → 覆盖导航 | 画布仍宽 334px、left=56；背景 inert，导航宽约 280px | `04b-390-navigation-overlay.png`、`04-layout.audit.json` |
| 05–06 | 实点导航外侧；恢复桌面尺寸 | 导航关闭 → 单列；桌面解除窄屏租用 | 通过 | `05-390-navigation-dismissed.png`、`06-desktop-restored.png` |
| 07 | 刷新已保存看板；只读连接中断 | v15 → 中文错误，内容仍在 | 通过 | `07-chinese-connection-error.png` |
| 08 | 同正文回退预览，确认请求在转发前阻断 | v15／v16 草稿 → 保存结果待核对 | 观察到 confirm 的 Request 暂停，明确 fail 后才解除；未写入 v16 | `08-confirm-blocked-before-forward.png`、`08-confirm.audit.json` |
| 09 | 取消处理中实点刷新及选择器 | 未知草稿 → 两次点击后均仍在看板 | 两控件 disabled 且 DOM 命中匹配；取消请求暂停 1705ms 后放行 | `09-cancel-busy.png`、`09a-after-disabled-refresh.png`、`09b-after-disabled-selector.png` |
| 10 | 放行一次取消请求 | 未知草稿 → 取消成功，已存 v15 | 本轮通过；cancel 1，无额外业务请求，truncated=false | `10-cancel-result-v15.png`、`09-cancel-clicks.audit.json`、`10-cancel-result.audit.json` |
| 11–12 | 清理拦截并只读刷新，恢复侧栏 | 已存 v15 → v15；原板列表 v9 | 本轮未新增保存版本；v16 草稿已取消 | `11-final-v15.png`、`12-final-restored-sidebar.png`、`12-cleanup.audit.json` |

09 的实际点击坐标分别为刷新 `(923,85)`、选择器 `(298.75,143.25)`，视口均为 1020×649。每次点击后独立保存截图和 AX。结果使用“仍在说明板＋取消成功提示＋已存版本不变＋草稿消失”共同判断；AX 曾省略取消状态段，故另外读取同次可见 DOM 原文，并以截图核对，不以忙态消失判成功。

本轮还观察到一次截图范围失配：390×844 的 DOM 被截成缩小画面，重申视口后截图恢复正常。原始 `04-390-navigation-overlay.png` 保留，验收用 04b。此现象提示浏览器控制证据也需核验，但不足以认定它就是历史 85 的根因。85 的旧截图和 INCONCLUSIVE 不回写为成功；本次 Request 阶段短暂停留也不冒充上轮 4000ms 网络延迟的完全同条件复现。

## 接续

已确认的窄屏与文案缺陷进入修复及验证完成状态；本批 13 个文件已完成逐文件审查，并与通过检查的源码／构建证据核对。交付保留仓库 hooks，合并前核验对应提交的 CI 与 QA，合入状态以 PR 回执为准。不再重复完整 B3 矩阵或为文案反复增加保存版本。85 保留为“历史未定因、本轮定点未复现”。下一阶段按固定候选整理 S4 尚缺的综合 QA 与 S5 用户验收入口，R-1 单独处理；不据本批 Git 交付关闭产品验收。
