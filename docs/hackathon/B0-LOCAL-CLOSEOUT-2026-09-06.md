# B0 本地连续执行收口

日期：2026-09-06。本轮状态：**DONE_WITH_CONCERNS / LOCAL_SCOPE_COMPLETE**；整体状态：**B0_PARTIAL / B1–B4_NOT_STARTED**。

后续 T08 更新：见[工具卡 DOM 增量报告](./B0-TOOL-CARD-DOM-2026-09-06.md)。组件层 8 状态/两档浏览器已补证，最新编译与装配回归为 14 项；UI-B02 原生故障事件全链仍 PARTIAL。下文保留 T01–T07 当时的 5 项装配、未验证项和停止证据，不以增量结果改写历史。

用户批准的 T01–T07 本地执行队列已逐项实施、验证并留证。此处“收口”是交付当前小样及真实缺口，不是把整个 B0、AI 模型或完整产品判为通过。总任务入口见[连续执行清单](./B0-EXECUTION-CHECKLIST-2026-09-06.md)；后续产品顺序继续沿用[原总计划](./PLAN-CLOSEOUT-2026-09-05.md)，不再新建另一套工作包。

## 1. 当前结论

| 队列 | 本轮交付与实际证据 | 结论与边界 |
|---|---|---|
| T01 范围与清单 | 固定隔离 worktree、既有 dirty 状态、顺序和授权边界 | DONE；未切换分支或整体收拢旧修改 |
| T02 方法包 | 3 文件整包 hash、引用/默认 roots 隔离、固定原生 registry/tool、按需资源读取与撤权拒绝 | 小样 PASS；原生组件实际读取，浏览器七问实际发现/登记但只调用查询工具 |
| T03 上下文 | 受理时绑定版本、当前后端条件/证据/预算重建；真实 compaction 组件测试；11 次本地 mock 请求逐次核对最新上下文与原截止时间 | 小样 PASS；组件压缩使用 HTTP fixture，不能称浏览器完整方法调用/压缩 E2E |
| T04 控制面 | 固定 HTTP/WS/Fetch 清单；持续权限缺口修复；当前实例 51 项 live 检查和权限不可用探针通过 | 小样 PASS；单合成身份，不是生产多用户安全认证；MCP NOT ENABLED |
| T05 界面与 BI 接缝 | 无会话/模型不可用资产、标题预览与草稿退出确认、恢复/键盘；完整条件往返样例；原品牌、三档视口及深浅主题 | 承载小样 PASS；静态资产与 BI 条件标本不等于业务资产库或实际同条件查询 |
| T06 回归与收尾 | 164 Python / 133 Node；5 项产物与真实装配测试原目录、干净目录各通过；完整类型、Ruff、逐字节一致；临时服务停止 | DONE；远端 CI、新机器空缓存安装未运行 |
| T07 当前入口与后续清单 | 同步本报告、任务清单、总计划、验收计划、UI 规格及插件说明；保留历史失败 | DONE；未执行 B1–B4 或 Git/部署交付 |

## 2. 可追溯证据

工作位置：`.worktrees/hackathon-mission-mvp`；本地分支 `codex/architecture-warehouse-plan-closeout`，HEAD `de2d785f4e0c7abe7fcd8fbb39be7d8c5a0c9642`。这些是本地未提交变更，不是已发布版本。

固定 DSH：`d347e703908d0406b7a7ef80e3a0e594d86b2215`；本次 Node `v24.19.0`、Python `3.14.4`。独立 FastAPI/SQLite 状态与只读小型合成 DuckDB；未连接归档真实库。

最终统一入口：

```bash
node scripts/dsh-b0/pipeline.mjs --check --python /ABSOLUTE/PYTHON3.14
```

2026-09-06T08:45:12.997Z 完成；干净目录 `clean-build-DPNRQY`。164 项 Python 有 1 项 warning，不声称零告警；133 项 Node 和 5 项装配测试无失败/跳过。5 项装配在原目录和干净目录各执行一次，不累加成 10 个不同测试。`--check` 不安装依赖；真实 loader 短暂占用的 4316 也已释放。

原始报告位于 `.context/dsh-b0/runtime-qCzzrY/`，该目录为本地私有运行证据，不纳入公开交付：

| 文件 | 结果 |
|---|---|
| `asset-ui-1788683417204.json` | 6 组资产/接缝检查 PASS；无活动会话、模型 mock 不可用、局部编辑/恢复、键盘及 390/768/1440 视口；业务 run 新增 0 |
| `gateway-smoke-report-1788683538319.json` | 当前固定 HTTP/WS/Fetch 正反例 51 PASS / 0 FAIL；不发送合法问题 |
| `permission-probe-1788683485392.json` | 当前 kernel 不可用时 HTTP 403、已有 WS 停止输出；finally 恢复原 PID |
| `native-ui-1788683693808.json` | 原生七问 PASS；刷新恢复不重派发，两个持久存储及独立退出证据相互一致 |
| `theme-ui-1788684166293.json` | 8 组深浅主题/视口与资产对比度检查 PASS；真实展开并可见的最后一张工具卡，无横向溢出或状态栏遮挡 |
| `closeout-evidence-1788684402569.json` | 记录报告 hash、最终构建快照、11 次模型 wire 上下文/截止时间核对及清理证据 |

最后三类浏览器报告中的 4 个插件产物 hash 均与最终干净构建一致（资产、原生七问、主题报告）。控制面/权限报告不是插件 hash 报告，其关联以当前 runtime 与源脚本为准，不把未记录 hash 写成已比对。

| 产物 | SHA-256 |
|---|---|
| `index.js` | `e16bfe25f7a1453d322bbfe5aac179df278bf69c466a6f072b6d27026e34dd70` |
| `tool.js` | `a6c44fc43883cb760a1931c611b28cd6d5958cc36a5739c3b66385e4d38f0740` |
| `skills.js` | `a7c90e2fac65fc7f940bfd27947f8626522fffb043364feca8cb60cdf9ce3188` |
| `client.js` | `85adb016fa059cb8774c2a1febdb40de3fb4da7e46c9bdf958d6c21dfd0da208` |

## 3. 七问与方法/上下文证据边界

原生发送/停止均通过真实 UI，不由验证脚本伪造点击结果。七次业务状态依次为 `SUCCEEDED / SUCCEEDED / CANCELLED / SUCCEEDED / FAILED / FAILED / SUCCEEDED`；其中第 5 次是固定服务错误，第 6 次是受控 Host 崩溃，两个 FAILED 正是期望结果。

- 每问 1 个原始派发；第一次与第二次完成后，后续新问题仍可正常受理。
- 第 3 问期间重启 FastAPI，保留原 run、attempt、request、截止时间，再经原生停止进入 CANCELLING，确认退出后才 CANCELLED。
- 第 6 问受控重启 Host；原生持久记录标 interrupted，业务账本保留失败，不伪装完成；第 7 问正常恢复。
- 四个成功结果来自真实只读合成 worker：每个独立退出、物理 lease 可重新独占、资源槽释放。不是模型自由生成数字。
- 原生历史、业务投影与刷新恢复核对后仍只有 7 个 run；原 202 重放一致、换输入 409、不新增原生问题。

新 preset 只登记 `analytics_b0_query`、`analytics_b0_skill_resource`、`skill` 三个工具。停机后的脱敏 mock 记录共 11 次请求，全部具有与 SQLite 同 run/request/attempt 及原截止时间一致的最新后端上下文；第一步存在原生 Skill 发现提示。包版本为 `09563c4eab580f693215af61b412834475858ae5e289ce49ab85df9baaaf315f`。

方法读取与实际压缩另在固定 DSH 原生 loop / Skill registry / tool / BasicCompactionEngine 组件中执行，包括错误数字、虚构批准、无限预算摘要及撤权拒绝。浏览器七问没有要求 mock 调用方法工具，也没有触发浏览器压缩；两层证据明确分开。详细实现及权限修复的红/绿证据见[方法与权限报告](./B0-METHOD-CONTROL-VALIDATION-2026-09-06.md)、[控制面清单](./B0-CONTROL-MANIFEST-2026-09-06.md)。

## 4. UI-B01–07 当前逐门状态

| 门 | 当前状态 | 限制 |
|---|---|---|
| UI-B01 安全进入 | PASS（B0 固定主会话） | 自动进入/刷新有证据；不是正式多用户工作区配置 |
| UI-B02 原生工具卡 | PARTIAL | 版本化成功 payload、真实展开卡和跨视口通过；加载中/工具失败/未知版本的完整 DOM 组合仍未实测，不能拿模型 500 代替工具卡失败 |
| UI-B03 原生操作映射 | PASS（七问子集） | 一一映射、停止、恢复和新问数通过；不是完整 D3 所有提交边界/队列形态验收 |
| UI-B04 固定驾驶舱 | PASS（承载小样） | 不依赖会话/模型；仅一个标题 UI 偏好，非正式资产持久化、组合或 AI 编辑 |
| UI-B05 控制面 | PASS（固定清单/单合成身份） | 当前操作重新验权；不是生产权限目录、多用户渗透审计 |
| UI-B06 旧 BI 接缝 | PASS（合同往返小样） | 固定完整条件、无脚本页面、刷新/历史/旧状态隔离已证；真实 run 同条件 BI 解析器未实现，入口明确叫“条件往返样例”，旧 BI 仍拒绝 |
| UI-B07 品牌/可用性 | PASS（新增插件承载） | 原始素材和比例不变，主题继承 DSH；390/768/1440、44px 操作区、Tab 双向封闭、Esc 确认/焦点归还、深浅文字对比度和减少动画分支通过；不代表整个上游界面全量 a11y 审计 |

驾驶舱素材使用用户原 PNG Logo 与帽子 favicon 的固定字节，经品牌插槽和局部 CSS mask 着色，未重画。旧 Vue 路由/全局筛选/store 未修改；BI 样例不执行查询，也不冒充驾驶舱 25% 指标的同条件结果。`browse` 实际核对促成了焦点归还和 Tab 边界修复，并要求工具卡处于展开可见状态；未改全局 CSS 或上游壳。

## 5. 失败记录与未关闭项

- **历史监督器退出：OPEN / NOT REPRODUCED。** 已有被动退出记录与专项验证；本轮没有再次自发出现该故障。受控 SIGKILL/正常 SIGTERM 不等于定位了历史根因，不关闭稳定性关卡。来源见[专项复查](./B0-SUPERVISOR-RECHECK-2026-09-06.md)。
- **UI-B02 完整状态组合仍 PARTIAL。** 已交付成功态和合同拒绝测试，不把未执行的 DOM 状态补成 PASS。
- 方法/compaction 的真实浏览器整链、多工具结果、正式资产/刷新与 BI 业务绑定均未验收；前两者已有分层组件证据，其余按 B1–B3 产品合同继续。
- 初次浏览器七问在前两问成功后，被旧验证器“只有一个工具”及“所有 user-role 都是用户发问”的断言中止。修正断言后严格核对原两问及原问题文本，从第三问继续，未重发或清库；两份 FAIL 报告 `native-ui-1788683547761.json`、`native-ui-1788683634743.json` 保留。
- 资产测试发现的焦点归还/Tab 离开弹层已修复并复验。首次主题截图包含原生折叠组中的不可见卡；最终检查增加可见性和遮挡断言并独立留图，早期结果不作为可见工具卡验收。
- 早期持续权限 FAIL、既往资源/进程故障与当时失败计数原样保留。不能以最后一轮 PASS 删除历史证据或推导生产可靠性。

上述限制决定整体仍为 B0_PARTIAL。未更换 runtime、fork 上游、启用第二套 Agent Loop、增加通用路由/动态插件平台或常驻监控。

## 6. 清理与后续交付清单

本轮最后实例于 2026-09-06T08:44:12.144Z 正常 SIGTERM 收尾，Host 退出 0；网关和监督器执行会话均退出 0。最终核验 4315–4319 无监听，监督器/网关/Host/kernel 的原 PID 全部消失。测试标签页 6 已关闭，既有空白页 1 保留。此前本轮中间实例也已各自停止；旧 5173/8000、CRM、真实库、ETL 未启动或改动。

后续顺序留在原计划，不自动执行：

1. **B0 遗留闭合**：UI-B02 的完整工具状态 DOM 用例；历史监督器问题只在新证据或明确的有界复查方案下推进，不无依据反复重跑。
2. **B1 / E-T1 + W2**：三查询族、完整条件/退款/首购/成熟窗口及手工金标准；先小合成数据，明确容量与资源预算。
3. **B2–B3 产品闭环**：正式资产与任务、连续追问、可组合驾驶舱、实际同条件 BI、人群/批准快照和当前结果绑定；补完整 V/P 矩阵。真实模型另需账号、模型与费用 cap。
4. **多人与容量**：按 V1/V2/W1–W5 做合成身份/多品牌店铺/5 请求调度、分级容量及 ETL 正确性；不默认迁移状态库或搬真实大库。
5. **独立 Git 与发布**：审查本轮文件集合 → 经授权分批 commit → 经授权 push/PR → 远端 CI → 经授权 merge/部署/网址提交。现有 dirty 修改不整体打包；本轮未执行这些动作。

没有对外消息、付费模型、真实数据库读写、ETL、守护进程安装或数据删除。未运行的业务 UAT、新机器复现与远端 CI 均不标通过。

收口文档检查：11 份当前/历史指针文档的 143 个本地链接目标均存在，文档与新增验证脚本空白检查、`git diff --check` 通过；暂存区无变更，分支与 HEAD 不变。未将这个文档检查称作新的业务或运行验收。
