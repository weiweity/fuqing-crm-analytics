# P13 · 真实 AI 样本与比赛链路验收

- 状态：**PARTIAL**
- 执行日期：2026-09-19
- 分支：`feat/free-html-library-chrome` HEAD `5637472a`
- 现役：6677 PID **37726**，`--page-http on` → 18091
- 模型：UI DeepSeek-V41-Flash Max；目录 `deepseek-official` / `deepseek-flash`
- **未使用预制 HTML。**

## 壳

已 reload。首页窄栏 + chips。截图 `.context/checks/p13-20260919/03-home.png`。

## 会话修复后再跑

`e0931457` / `5637472a`：生成打进可见会话，waiter 与 `session.prompt` 共用 `page-gen` id。

| 场景 | 输入 | 结果 |
|---|---|---|
| 经营杂志复盘页 | 「做一张本周收入波动复盘页」 | **仍失败**（185s 同一超时文案）。但 `chatHasPrompt=true`：侧栏出现「生成本周收入波动复盘页面」。右侧 Agent 在改 `free-html-page/app.js`（工作区编码），**没有**调用 `free_html_page_generate`。截图 `05-after-generate.png` |
| 自定义交互图表 | — | **NOT_RUN** |
| 既有页面局部修改 | — | **NOT_RUN** |

会话选择修复已由 `chatHasPrompt` 证实。阻塞改成：现役会话处于 Agent Team / 工作区改文件模式，模型不走页面交付工具。资料库 waiter 仍然只能等该工具回执。

## 未跑

D48 真模型、多视口、D6/D9/回滚。不捏造提升率。

## 证据

| 检查 | 结果 |
|---|---|
| 未登录 GET / | 401 |
| 修复后提示进入可见会话 | 是（侧栏新会话名 + chatHasPrompt） |
| 工具 `free_html_page_generate` | 185s 内无回执 |
| 预制 HTML | 未用 |
