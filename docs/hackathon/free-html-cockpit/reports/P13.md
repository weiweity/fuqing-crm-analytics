# P13 · 真实 AI 样本与比赛链路验收

- 状态：**PARTIAL**
- 执行日期：2026-09-19
- 分支：`feat/free-html-library-chrome` HEAD `f2b61717`
- 现役：6677 PID **52407**（已 reload 含 REGISTERED_TOOLS 修复），`--page-http on`
- **未使用预制 HTML。**

## 本轮（工具族注册之后）

硬刷新后的插件已把 `free_html_page_generate` 列入与组板同一套 REGISTERED_TOOLS。再开新会话 → 驾驶舱 → 自由页面 →「做一张本周收入波动复盘页」。

| 观察 | 结果 |
|---|---|
| 提示进入可见会话 | 是（侧栏「本周收入波动复盘页」，`chatHasPrompt=true`） |
| 模型怎么说 | 「`free_html_page_generate` 在当前会话的工具清单里不存在（我这边只有文件、Bash、搜索、团队等工具）」 |
| Agent 实际 | Agent Team 写 `free-html-page-page-gen-581502c9/...`，Bash 自检 |
| 工作台 | 187s 超时，无 iframe |

截图：`.context/checks/p13-20260919/12-after-generate.png`。正文：`s1-standard.json`。

后两场 **NOT_RUN**。

## 结论

插件侧「和组板同一套名单」已经上现役。P13 仍失败，因为 **Agent Team / 工作区会话的模型可见工具只有文件和 Bash**，Host 注册的插件工具进不了这份清单。资料库 waiter 只认工具回执，所以文件里的 HTML 不算交付。

要跑通，需要 DSH 会话把插件工具（至少 `free_html_page_generate`）发给当前 Agent，或换一种非 Agent Team 的对话面。这已经超出本插件 REGISTERED_TOOLS 能改的范围。
