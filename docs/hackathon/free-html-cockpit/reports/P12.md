# P12 · 集成验收

- 状态：**PARTIAL**
- 执行日期：2026-09-18
- 执行者：主协调 Agent
- 集成工作树：`/Users/hutou/Desktop/ai-engineering/历史项目/fuqin-date/fuqing-crm-analytics-free-html-P12`
- 分支：`codex/free-html/p12`（从 `b45a27bb` 新建，**未 commit / 未 push / 未 merge**）
- approved-plan SHA256：`47fa5bfe482cb9bd93f4f02a7c62c6933d0242b5204ea95f2b912edc603a6ffb`
- 现役 6677：PID **8758**，未切、未 reload

## 六路核对

| Lane | 报告 | owner-scope | 本地 commit | 本轮复跑 |
|---|---|---|---|---|
| A | LANE-A.md DONE | 合同+存储 | `8d1884bd` + `6a11fedf` | 13 page + 60 board + contract check |
| B | LANE-B.md DONE | 运行时+T0 | `02af826c` | 先前 27 含 Chrome T0 |
| C | LANE-C.md PARTIAL | 合成桥 | `40e048ce` | 15 Python + 15 Node |
| D | LANE-D.md DONE | D48 编辑 | `fdb07749` | 34 Node |
| E | LANE-E.md PARTIAL | 资料库 UI | `6271b43f` | **58/58** Node |
| F | LANE-F.md 待写（WIP） | leave/navigation 有代码 | 未 commit | 离开/epoch 单测在 P12 树跑过；无交接报告所以 P11 不能 DONE |

破坏性合同：未发现第二套 schema_version / 绑定态 / 操作集。B–C 仍有 `frozen-contract-v0.json` 手写常量；P12 live adapters 已改用 A/B/C/D 模块，但 C 的 `createSyntheticAccess` 仍是合成结果库（声明 mock）。

F 改了 `library-board-client.mjs`（epoch/离开接缝）。这超出「只新建 leave/navigation」字面，但对 D43/D44 是必要宿主接缝；已带进 P12 树，未当越界打回。

## 移植

按 A→B→C→D→E→F 拷入 P12 树，目录无互相覆盖：

- A `contract/` + `page_documents*`
- B `runtime/` `resource/` `preview/`
- C `bridge/` + `page_result_access*`
- D `source-index/` `patch/` `edit/`（去掉 `tests/free-page-edit.test.mjs` 再导出）
- E `src/client/free-html-library/` + 资料库/主题改动
- F `leave/` `navigation/` + `library-board-client.mjs`

接缝代码：

- `src/client/free-html-library/live-adapters.mjs`：B `buildSrcdoc` + `allow-scripts`、D `locateSelection`/`createPatchPreview`、C `FORBIDDEN_OPS`、资产走 D memory store 索引（Python `page_documents` 仍是 HTTP SSOT）
- `create_competition_app(..., page_state_dir=)` 才挂 `page_documents_router`；默认不碰 6677
- `pipeline.mjs`：`page-contract.mjs --check`；扫描目录加上 `src/free-page`

E 的 `createMockPageAdapters` 仍给 58 项 UI 单测用。生产接线入口是 `createLivePageAdapters`。

## 通过项（本轮命令）

cwd = P12 worktree。Node **v24.19.0**。Python **3.14.4**。

| 检查 | 结果 |
|---|---|
| `python3.14 scripts/run_backend_tests_bounded.py backend/tests/test_page_documents.py` | 13 passed，exit 0 |
| 同上 `test_page_result_access.py` | 15 passed，exit 0 |
| `node scripts/dsh-b0/page-contract.mjs --check --python …/python3.14` | exit 0，sha256 `86ad3cf4…` |
| D 四文件 `node --test` | 34 passed |
| C `bridge/*.test.mjs` | 15 passed |
| E 自由页+主题+资料库（E 树） | **58 passed** |
| P12 `p12-live-adapters.test.mjs` | **2 passed**：未绑定生成、sandbox srcdoc、D 定位不扩整页、C 禁 SQL、干净选区不是 dirty |
| F leave/navigation 单测（随 P12 树） | 通过（含 D42 干净选区不离开） |
| ruff `analytics_competition_app.py` | exit 0 |

浏览器：Lane B T0 仍以 Chrome/153 Headless 三项门禁为准（P12 未重开 headed 全旅程）。E 视觉夹具 PNG 在 E 树 `reports/lane-e-evidence/`。

## 阻塞 / 未运行

- **LANE-F.md 待写（WIP）**：P11 不能 DONE。F 代码已进 P12 树，交接报告仍是 WIP。
- **完整 `pipeline.mjs --check` NOT_RUN**（干净重建/全量 B0 过重；只改了扫描入口）。
- **真浏览器生成→绑定→选区→D6/D9→重开/回滚 NOT_RUN**。有模块级 live adapters 与 A 的 HTTP 分路测试，没有 React+iframe+MessageChannel 的 Chrome 点选旅程。
- C 真实结果库 / 原生 Agent 新查询 NOT_RUN（synthetic 仍声明）。
- JS 资产层尚未 HTTP 打到 `page_documents`；A 的 SQLite 只在 Python TestClient。
- Noto Sans SC 未捆绑。DESIGN.md 普惠体句未改（需另授权）。
- P13 真实 AI **未开始**。

## 剩余接线

1. 给 E 生产入口注入 `createLivePageAdapters`，iframe 用 B `mountPreviewHost` 替换手写 srcdoc。
2. 给 `createPageSession({ adapter: { read, cancel } })` 接 C 的 Python `PageResultAccess`（需隔离 HTTP，不要切 6677）。
3. 补 LANE-F.md 后再把 P11 从 BLOCKED 改为有证据的 PARTIAL/DONE。
4. 有授权后再跑完整 pipeline 与 headed 集成。
