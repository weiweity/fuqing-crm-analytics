# P12 · 集成验收

- 状态：**PARTIAL**
- 执行日期：2026-09-18
- 执行者：主协调 Agent
- 集成工作树：`/Users/hutou/Desktop/ai-engineering/历史项目/fuqin-date/fuqing-crm-analytics-free-html-P12`
- 分支：`codex/free-html/p12`（基线 `b45a27bb`）
- 已提交 HEAD：`d0cd6cb9` + 随后 `pullPage` 本地提交
- 相对 `ac70cd21` 的接缝已本地提交；**未 push / 未开 PR**
- approved-plan SHA256：`47fa5bfe482cb9bd93f4f02a7c62c6933d0242b5204ea95f2b912edc603a6ffb`
- 现役 6677：PID **8758**，未切、未 reload；未启动自有演示实例

## 六路收件（2026-09-18 复核 git，不以登记 READY 代替）

共同基线 `b45a27bb9022057ce37aa9123927e8b4663a2720` == `origin/main`。各路 extra dirty 仅为施工包 / 提示词，不含未提交业务源码。

| Lane | worktree | branch | 交接 HEAD | 方案 SHA | 累计提交 | 证据 |
|---|---|---|---|---|---|---|
| A | `…/fuqing-crm-analytics-free-html-A` | `codex/free-html/lane-a` | `6a11fedf` | 同上 | `8d1884bd` + `6a11fedf` | LANE-A.md DONE；本轮未重跑 13 page |
| B | `…-B` | `codex/free-html/lane-b` | `02af826c` | 同上 | `02af826c` | LANE-B.md DONE；Chrome T0 沿用先前 27 pass，本轮未重开 headed |
| C | `…-C` | `codex/free-html/lane-c` | `40e048ce` | 同上 | `40e048ce` | LANE-C.md PARTIAL 合成桥；本轮 P12 树 bridge 15 pass |
| D | `…-D` | `codex/free-html/lane-d` | `fdb07749` | 同上 | `fdb07749` | LANE-D.md DONE；本轮 34 owner + hash 对照 35 pass |
| E | `…-E` | `codex/free-html/lane-e` | `6271b43f` | 同上 | `6271b43f` | LANE-E.md PARTIAL mock adapters；UI 测在 P12 用 mock store |
| F | `…-F` | `codex/free-html/lane-f` | `a3a9b428` | 同上 | `a3a9b428` | LANE-F.md PARTIAL；leave/nav 本轮 110 项切片含 F 全绿 |

破坏性合同：未发现第二套 `schema_version` / 绑定态 / 操作集。C 的 `createSyntheticAccess` 仍是合成结果库（声明 mock）。JS 资产层仍是 D memory store；A 的 SQLite 只在 Python TestClient。

## 本轮接缝（相对 `ac70cd21` 的未提交改动）

1. **浏览器包可引用 live adapters**：`src/free-page/hash.mjs` 同步 SHA-256，替换 D `source-index` / `patch` 的 `node:crypto`。DOM 测试 `platform:browser` 不再在 `impact.mjs` / `source-index` 解析失败。
2. **生产 live adapters 由宿主注入**：`createHostPageStore()` 只从 `index.tsx` 引入。`FreeHtmlLibraryApp` 默认 mock，DOM 测试不再静态打进 live 树。
3. **D42 beforeunload**（已在树）：`library-workspace.tsx` 只读 `library.hasUnsavedChanges()`。
4. **离开只走一个协调器**：宿主 `goConversation` 读 `htmlUnsaved`；自由页脏稿与看板脏稿同一套三选。FHL 内联三选只用于资料库内「回首页」，testid 为 `fhl-leave-*`。PENDING PATCH 走 D6 一次，本地 dirty 走 D9 一次。
5. **`perform(id)`**：`panel` + id 选该面板；conversation/session/close 回对话；library 留在 cockpit。
6. **`LeavePromptOverlay`** 使用 `PropsRuntime<'shell.overlay'>` 的 `usePanelInfo`，观察侧栏切面板只推进 epoch。
7. **页面路由挂载**：`create_competition_app(..., page_state_dir=)` 才挂 `page_documents_router`；默认不碰 6677。`backend/tests/test_p12_page_mount.py`。
8. **pipeline**：`page-contract.mjs --check` 已在 `pipeline.mjs`；扫描目录含 `src/free-page`（20 个 `*.test.mjs`，含新 `hash.test.mjs`）。已去掉 `tests/free-page-edit.test.mjs` 再导出。
9. **F 代码**：P12 工作区已同步 F `a3a9b428` 的 leave/navigation/`library-board-client`/`index.tsx`/`leave-prompt.tsx`。

## 审查检查点（commit 前 /review，本轮未授权提交）

审查对象：本轮未提交接缝，不是整棵 P12 相对 main 的全量 OCR 重跑。

| 路径 | 结论 |
|---|---|
| `src/free-page/hash.mjs` + `hash.test.mjs` | reviewed：对照 `node:crypto` 向量 |
| `src/free-page/source-index/index.mjs`、`patch/impact.mjs` | reviewed：仅换摘要实现 |
| `src/client/free-html-library/{store,FreeHtmlLibraryApp,create-host-page-store,*test*}` | reviewed：宿主注入 live、htmlUnsaved、D6/D9 |
| `src/client/library-workspace.tsx` | reviewed：D42 beforeunload |
| `backend/tests/test_p12_page_mount.py` | reviewed |
| F leave/*、`index.tsx` | 沿用 F 树 OCR 已修切片；本轮未再跑 ocr-delegate 全量 |

未发现本切片 critical/high。整候选相对 main 的全量 OCR **未运行**，不能当作审查通过。

## 验证证据

cwd = P12 worktree。Node **v24.19.0**（`$HOME/homebrew/opt/node@24`）。Python **3.14.4**。`B0_BUILD_UPSTREAM` = 主仓 `.context/dsh-b0/upstream`。

| 检查 | 命令 | 环境 | 结果 | 证据 |
|---|---|---|---|---|
| 同步 SHA-256 | `node --test …/hash.test.mjs` + D 四文件 | Node 24 | **35 passed**，exit 0 | stdout |
| pipeline 扫描的 67 个 `*.test.mjs`（不含 isolation-probe） | `node --test $(67 files)` 无 `lib/`、默认 python3 | 主仓 upstream | **454 pass / 4 fail**：3 个要 `lib/`，1 个 offline eval 落到系统 python3.9 | 本机 stdout |
| 补 `build.mjs` + `PYTHON=python3.14` 后重跑失败文件 | competition-board-dom + competition-agent + fail-closed-dom | 刚编的 `lib/`（gitignore） | **39 passed**，exit 0 | stdout |
| plugin typecheck | `node build.mjs` 钉死 upstream | Node 24 | host+client **passed**（含 live `mountPreviewHost`） | stdout |
| library-workspace D42 | 含在上一轮 50 pass 套件 | jsdom | beforeunload 测试 pass | stdout |
| leave + C 桥 + B 运行时（不含 isolation-probe） | `node --test` leave/* navigation/* bridge/* preview/* resource/* 部分 runtime + schema | Node 24 | **110 passed**，exit 0 | stdout |
| 页面 Python | `python3.14 scripts/run_backend_tests_bounded.py` 三个 page 文件 | bounded pytest | **30 passed**，exit 0 | `.context/checks/20260918T092512787490Z/summary.json` |
| ruff | 上列 page Python | ruff 锁 | exit 0 | stdout |
| 离线合同 | `node scripts/dsh-b0/page-contract.mjs --check --python …/python3.14` | 离线 | exit 0，sha256 `86ad3cf4d71b05cb827a52f38b568ae3e0ed02ea7312a9dd3b6d848971e0ef3a` | stdout |
| pipeline 扫描 | 按 `pipeline.mjs` 目录列出 `*.test.mjs` | 只读 | 共 68；`src/free-page` **20**；`src/client` **28** | stdout |
| 完整 `pipeline.mjs --check` | 未执行 | 干净重建/全量 B0 过重 | **NOT_RUN** | — |
| Chrome T0 isolation-probe | `node --test …/isolation-probe.test.mjs` | Chrome `/Applications/Google Chrome.app`；自有端口，避开 6677 | **1 passed**，exit 0，约 8s | stdout |
| 真浏览器生成→绑定→选区→D6/D9→重开 | 未执行 | headed Chrome | **NOT_RUN** | — |
| C 真实结果库 / 原生 Agent 新查询 | 未执行 | synthetic 仍声明 | **NOT_RUN** | — |
| JS → `page_documents` HTTP | 未接线 | memory store | **NOT_RUN** | — |
| 原生侧栏脏稿留页 | 宿主 `selectPanel` 无否决钩 | DSH | **不可用** | LANE-F.md |

先前失败：把 `createLivePageAdapters` 静态打进 `FreeHtmlLibraryApp` 后，esbuild 报 `Could not resolve "node:crypto"`。本轮用同步 `hash.mjs` 修复后 **17/17** 再跑通过。不把后来绿灯写成从未失败。

## 未完成

- 资料库 **mock** 路径仍是 React `srcdoc` iframe。**live** 路径（`adapterKind === p12-live`）已挂 B `mountPreviewHost`（`chrome:false`，iframe testid `fhl-iframe`，load 时 `session.attach`）。jsdom 证明宿主节点与 sandbox iframe；headed 生成→绑定→D6/D9 仍 **NOT_RUN**。
- `documents.pullList` / `pullPage` 可打隔离 `page_documents`（mock fetch；`:6677` 拒绝）。生产 `createHostPageStore()` 仍未传入 documentsHttp。C `PageResultAccess` 仍无 HTTP 路由，JS 继续用合成桥。
- `openCockpitPanel` 进入路径仍不经离开协调器。
- Noto Sans SC 未捆绑。
- P13 真实 AI 未跑，见 `reports/P13.md`。

## 进程

本轮未启动临时 HTTP/Chrome。现役 6677 PID **8758** 未变。
