# P12 · 集成验收

- 状态：**PARTIAL**
- 执行日期：2026-09-18
- 执行者：主协调 Agent
- 集成工作树：`/Users/hutou/Desktop/ai-engineering/历史项目/fuqin-date/fuqing-crm-analytics-free-html-P12`
- 分支：`codex/free-html/p12`（基线 `b45a27bb`）
- 已提交 HEAD：`9fb89a75` 账本 ← `7e135755` headed/pipeline ← `9c12eee6` 生产接缝；其前为 `348fad44`
- **未 push / 未开 PR**
- approved-plan SHA256：`47fa5bfe482cb9bd93f4f02a7c62c6933d0242b5204ea95f2b912edc603a6ffb`
- 现役 6677：PID **8758**，未切、未 reload；未启动自有演示实例

## 六路收件（2026-09-18 复核 git）

共同基线 `b45a27bb9022057ce37aa9123927e8b4663a2720` == `origin/main`。

| Lane | worktree | branch | 交接 HEAD | 累计提交 | 证据 |
|---|---|---|---|---|---|
| A | `…/fuqing-crm-analytics-free-html-A` | `codex/free-html/lane-a` | `6a11fedf` | `8d1884bd` + `6a11fedf` | LANE-A.md DONE |
| B | `…-B` | `codex/free-html/lane-b` | `02af826c` | `02af826c` | LANE-B.md DONE |
| C | `…-C` | `codex/free-html/lane-c` | `40e048ce` | `40e048ce` | LANE-C.md PARTIAL 合成桥；P12 已加隔离 HTTP |
| D | `…-D` | `codex/free-html/lane-d` | `fdb07749` | `fdb07749` | LANE-D.md DONE |
| E | `…-E` | `codex/free-html/lane-e` | `6271b43f` | `6271b43f` | LANE-E.md PARTIAL mock adapters |
| F | `…-F` | `codex/free-html/lane-f` | `a3a9b428` | `a3a9b428` | LANE-F.md PARTIAL；leave 已进 P12。原生侧栏 `selectPanel` 没有 veto API，不能假装已拦截 |

破坏性合同：未发现第二套 `schema_version` / 绑定态 / 操作集。C 的 `createSyntheticAccess` 仍给 C 自己的模块测试用；P12 生产 live 路径的 `data.read` 走隔离 HTTP。

## 本轮接缝（相对 `348fad44`，已落在 `9c12eee6` / `7e135755`）

1. **生成入口**：`p12-live` 的 `generate()` 只接受原生 Agent 返回的 `{html,css,js}` 源码包。没有包就失败并保留输入，**不会**回退到 `samplePackage()`。宿主 `index.tsx` 把 `createNativePageGenerate` 接到 `ctx.remote.session.prompt`。
2. **持久化**：`documents.generateAndConfirm` / `patchPreview`+`confirmPreview` / `savePreview`+`confirmPreview` / `rollbackPreview` / `pullPage` / `pullHistory` 打隔离 `page_documents` HTTP；确认带 `Idempotency-Key`。URL 含 `:6677` 抛 `REFUSED_LIVE_PORT`。未配置 HTTP 时返回 `http_not_configured`，不把内存 store 当成已保存。
3. **结果桥**：新增 `page_result_access_routes.py`，前缀 `/api/v1/analytics/page-result-access`。默认 competition app **不挂**；`page_result_access=` 或 `create_page_app(..., result_access=)` 才挂。JS live `bridge.readResult` 打该 HTTP，未配置则 `RESULT_HTTP_NOT_CONFIGURED`。
4. **侧栏**：F 离开协调器已在 P12。原生 DSH `layout.selectPanel` 仍无否决钩，脏稿点侧栏行**不能**留页。此限制已记录，未假装实现。
5. **P13**：无模型/额度授权，保持 **BLOCKED**。未用预制 HTML 代替真实生成。

## 验证证据

cwd = P12 worktree。Node **v24.19.0**。Python **3.14.4**。`B0_BUILD_UPSTREAM` = 主仓 `.context/dsh-b0/upstream`。

| 检查 | 命令 | 结果 |
|---|---|---|
| live adapters + store + generate-context | `node --test` 上述测试文件 | **22 passed** |
| FreeHtmlLibraryApp DOM | `B0_BUILD_UPSTREAM=… node --test free-html-library.test.mjs` | **9 passed** |
| 结果 HTTP + 挂载 + C 模块 | `python3.14 -m pytest` 三个 page_result / p12_mount 文件 | **22 passed** |
| 页面 SQLite HTTP | `pytest backend/tests/test_page_documents.py` | **13 passed** |
| ruff | 新路由与挂载文件 | exit 0 |
| plugin typecheck + build | `node build.mjs` 钉死 upstream | host+client **passed** |
| `git diff --check` | 工作区 | 无空白错误 |
| headed Chrome 主链路 | `node --test --test-timeout=180000 p12-headed-journey.test.mjs` | **1 passed**，约 2.2s；端口 ≠ 6677 |
| 完整 `pipeline.mjs --check` | `B0_BUILD_UPSTREAM=主仓upstream node scripts/dsh-b0/pipeline.mjs --check --python /abs/python3.14` | **FAIL** 于 `scripts/dsh-dev`：`brand.test.mjs`（`shine-mage.png` 仍是 git-lfs 指针，sha `656a582c…`，不是位图 `21b82737…`）与 `diagnose.test.mjs`（`lfs_pointer`）。本轮未改品牌文件，未执行 `git lfs pull`。P12 源码切片另记 |
| 真模型三场景 | 无额度授权 | **BLOCKED**，见 P13.md |

headed 主链路（隔离 loopback，nativeGenerate 返回 Agent 源码包，documents/result 走同端口 HTTP）：生成 → 预览包校验 → 结果桥 `data.read` → 点选 `n_title` → D6 确认 → D9 保存 → 刷新重开 → 回滚 → 脏稿离开三选。6677 PID **8758** 未 reload。

## 未完成（因此不能写 READY_FOR_SHIP）

- 完整 `pipeline.mjs --check` 本轮 **FAIL**：卡在 dsh-dev 品牌 LFS 指针，未进入后续 page pytest / competition node / clean rebuild。P12 切片已另跑：page-contract sha256 `86ad3cf4…`、page pytest **35 passed**、相关 node **120 passed**、headed **1 passed**、typecheck+build passed。
- 因此不能把全量 `--check` 写成通过。
- 生产 `createHostPageStore()` 要从环境变量 `PAGE_DOCUMENTS_HTTP_BASE` / `PAGE_RESULT_HTTP_BASE` 显式指向**隔离** HTTP；未配置则生成/保存/读桥失败，而不是写进 6677。
- 原生 Agent `session.prompt` 若只入队、不返回 JSON 源码包，生成会报 `NATIVE_GENERATE_UNAVAILABLE`。这是故意的：宁可失败，也不用 `samplePackage()`。
- 原生侧栏行点击没有 veto。
- Noto Sans SC 未捆绑。
- `openCockpitPanel` 进入路径仍不经离开协调器。
- P13 真实 AI 三场景 BLOCKED。
- 整候选相对 main 的全量 OCR 本轮未重跑。

## 进程

headed 用完即关。现役 6677 PID **8758** 未变。未碰 131GB DuckDB、DSH 上游、浮动依赖。
