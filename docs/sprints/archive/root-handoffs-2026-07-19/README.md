# 根目录 HANDOFF 归档说明（2026-07-19）

原根目录 `HANDOFF-TO-CODEX-*.md` 已迁出并清理。

| 主题 | 处置 |
|---|---|
| Admin Upload v1–v4 / Sprint1 | **产品撤回** — 正文已删除；勿恢复为默认路线 |
| RFM cache miss handoff | 已合主线；过程文删除，见 CHANGELOG / history |
| 规则 | 新 handoff 只写 `docs/sprints/`，合 main 后进 `archive/` |

`.gitignore` 仍匹配 `HANDOFF-TO-CODEX-*.md`，防止再堆回仓库根。
