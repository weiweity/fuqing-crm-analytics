# 芙清 CRM - 测试报告

> **最后更新**: 2026-05-04  
> **Python**: 3.13.12 | **pytest**: 9.0.3

---

## 测试结果总览

| 项目 | 结果 |
|---|---|
| 后端单元测试 | ✅ **140 passed, 0 failed, 8 skipped** |
| E2E 测试 | ✅ 1 个 spec，覆盖老客健康页面 |
| 前端构建 | ✅ `npm run build` 通过 |

> 8 skipped 为 API 集成测试，需要 HEALTH_API_KEY + DuckDB 数据库文件

---

## 测试文件详情

### test_calculations.py（38 个测试）- 统一计算规则

| 测试类 | 测试数 | 覆盖 |
|---|---|---|
| `TestYoyAbsolute` | 13 | 绝对值 YOY：正/负/零增长、除零保护、None 处理、字符串输入、精度 |
| `TestYoyRatio` | 7 | 占比 YOY：百分点差、零值、None |
| `TestYoyRepurchaseRate` | 1 | 回购率 YOY（yoy_ratio 别名一致性） |
| `TestMomAbsolute` | 5 | 环比绝对值 |
| `TestMomRatio` | 4 | 环比百分点差 |
| `TestSafeRatio` | 5 | 安全除法：零保护、自定义默认值 |
| `TestUnitConversion` | 3 | 百分比 ↔ 小数转换、roundtrip |

### test_filters.py（27 个测试）- 过滤条件构造器

| 测试类 | 测试数 | 覆盖 |
|---|---|---|
| `TestOrderFilters` | 11 | valid_order 三重条件、gmv_base、渠道 IN/NOT IN、时间范围、会员 |
| `TestFilterBuilder` | 11 | 动态构造器：GMV/GSV 切换、时间、渠道、链式调用、lookback |
| `TestAmountExprBuilder` | 6 | GSV CASE WHEN、GMV SUM、自定义列、条件求和 |
| `TestExpandChannels` | 5 | 组合渠道展开、去重、UI→DB 映射 |

### test_time.py（22 个测试）- 时间周期构造器

| 测试类 | 测试数 | 覆盖 |
|---|---|---|
| `TestDateRange` | 3 | start_dt/end_dt 属性、cutoff |
| `TestPeriodBuilderWTD` | 5 | WTD：周一开始、昨天结束、上周对比、前年同周 |
| `TestPeriodBuilderMTD` | 5 | MTD：月初开始、去年同月 |
| `TestPeriodBuilderYTD` | 3 | YTD：1月1日开始 |
| `TestPeriodBuilderFree` | 4 | 自定义时间范围、年份平移 |
| `TestPeriodBuilderLookback` | 2 | 90 天 / 30 天回溯 |
| `TestPeriodBuilderMom` | 2 | 环比周期长度匹配 |
| `TestPeriodBuilderYoY` | 2 | 同比年份平移 |
| `TestNormalizeDateString` | 3 | date/string/datetime 归一化 |

### test_channels.py（12 个测试）- 渠道漏斗定义

| 测试类 | 测试数 | 覆盖 |
|---|---|---|
| `TestChannelFunnel` | 5 | 9 层漏斗、key 唯一性、购物金排除、优先级 |
| `TestChannelMapping` | 5 | DB↔UI roundtrip、赠品/U先 特殊映射 |
| `TestGetChannelDef` | 2 | 已知渠道查询、未知渠道降级 |

### test_segments.py（14 个测试）- RFM 分群注册表

| 测试类 | 测试数 | 覆盖 |
|---|---|---|
| `TestSegmentRegistry` | 6 | 单例、ID 查询、名称、颜色、列表 |
| `TestRFMScoreSQL` | 3 | R/F/M 评分 SQL 生成 |
| `TestSegmentCaseWhen` | 3 | 8 象限 CASE WHEN、旧 10/11 已移除 |
| `TestThresholds` | 3 | 阈值值验证、优先级排序 |

### test_flow_service.py（6 个测试）- 人群流转服务

| 测试类 | 测试数 | 覆盖 |
|---|---|---|
| `TestFlowServiceImports` | 1 | 无硬编码 SEGMENT_MAP |
| `TestFlowMatrix` | 1 | 流转矩阵键结构 |
| `TestSegmentCount` | 3 | 9 个分群、旧 10/11 已移除 |

### test_exceptions.py（6 个测试）- 异常类型

| 测试类 | 测试数 | 覆盖 |
|---|---|---|
| `TestExceptionTypes` | 6 | HTTP 状态码映射、继承关系、自定义错误码 |

### test_api_integration.py（10 个测试）- FastAPI 集成

| 测试类 | 测试数 | 覆盖 | 状态 |
|---|---|---|---|
| `TestAppStartup` | 2 | 路由注册、OpenAPI schema | ✅ |
| `TestMetricsAPI` | 3 | overview/trend | ⏭️ 需 DB |
| `TestAudienceAPI` | 1 | audience table | ⏭️ 需 DB |
| `TestGeoAPI` | 1 | geo distribution | ⏭️ 需 DB |
| `TestCategoryAPI` | 1 | category distribution | ⏭️ 需 DB |
| `TestFlowAPI` | 1 | flow matrix | ⏭️ 需 DB |
| `TestCustomerHealthAPI` | 1 | health overview | ⏭️ 需 DB |

---

## 覆盖缺口（仍需补充）

| 模块 | 优先级 | 说明 |
|---|---|---|
| `health/*.py` | P1 | 老客健康 6 个子服务（overview/repurchase/tiers/rfm/conversion/promotion） |
| `rfm_service.py` | P1 | RFM 计算核心逻辑（SegmentRegistry 迁移未完成） |
| `metrics_service.py` | P2 | 核心指标计算（已有 filter 集成，缺端到端测试） |
| `geo_service.py` | P2 | 地域分析 |
| `category_service.py` | P2 | 品类分析 |

---

## 修复记录

| 日期 | 修复 | 说明 |
|---|---|---|
| 2026-05-04 | `exceptions.py` | 废弃常量 → 数值字面量，消除 DeprecationWarning |
| 2026-05-04 | 新增 5 个测试文件 | calculations/filters/time/channels/api_integration |
