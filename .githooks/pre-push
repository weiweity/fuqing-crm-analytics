#!/bin/bash
# Pre-push hook: run tests before pushing to remote
# Install: git config core.hooksPath .githooks

echo "Running tests before push (parallel)..."
PYTHONPATH="$(git rev-parse --show-toplevel)" python3 -m pytest backend/tests/ -x -q -n auto 2>/dev/null
if [ $? -ne 0 ]; then
    echo ""
    echo "❌ Tests failed. Fix them before pushing."
    echo "   Run: PYTHONPATH=. pytest backend/tests/ -x -q"
    exit 1
fi

echo "✅ Tests passed."
