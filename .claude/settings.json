{
  "_comment": "Project-local Claude hooks only: retain pattern guards and per-file lint; no automatic Git changes, branch cleanup, service lifecycle, global memory scan, or rule generation. Behavior policy: AGENTS.md.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys, json, re; d=json.loads(sys.stdin.read() or '{}'); p=d.get('tool_input',{}).get('file_path',''); bad = re.search(r'(?:^|/)(?:\\.env|\\.env\\.local|\\.env\\.[^/]+\\.local|data/processed/.*\\.duckdb)(?:\\$|/)', p); sys.exit(2 if bad else 0)\""
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"\nimport sys, json, re\nd = json.loads(sys.stdin.read() or '{}')\ncmd = d.get('tool_input', {}).get('command', '')\n# Sprint 178: 拦截危险命令 (force push / rm -rf / mkfs / fork bomb)\nforbidden = [\n    r'\\bgit\\s+push\\s+.*--force\\b',\n    r'\\bgit\\s+push\\s+.*-f\\b(?!or)',\n    r'\\brm\\s+-rf?\\s+/',\n    r'\\bmkfs\\b',\n    r':\\(\\)\\s*\\{\\s*:\\|:\\s*&\\s*\\};:',\n    r'\\bdd\\s+if=/dev/(zero|random|urandom)\\s+of=/dev/(sda|nvme|disk)\\b',\n    r'\\bchmod\\s+-R\\s+777\\s+/',\n]\nfor pat in forbidden:\n    if re.search(pat, cmd):\n        print(f'[Sprint 178 PreToolUse Bash] 危险命令拦截: {pat}')\n        print(f'   command: {cmd[:200]}')\n        sys.exit(2)\n\""
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys, json, re, subprocess; d=json.loads(sys.stdin.read() or '{}'); p=d.get('tool_input',{}).get('file_path',''); \nif p.endswith('.py'):\n  try: subprocess.run(['ruff','check', p], check=False, timeout=30)\n  except: pass\nif re.search(r'backend/contracts/.*\\.py$', p):\n  print('[contract 改动] 提醒: 按 AGENTS.md 区分 B0/旧 CRM；使用对应离线契约检查，不自动启动服务或覆盖类型')\n\""
          }
        ]
      }
    ]
  }
}
