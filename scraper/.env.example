# DMP Scraper 环境变量模板
# 用法：cp scraper/.env.example scraper/.env，填入实际值
# 实际加载：scraper/core/sanity_check.py 通过 os.environ 读取
# （如需 dotenv 自动加载，请在调用前自行 source .env 或用 python-dotenv）

# ============================================================
# 飞书 webhook 告警
# ============================================================
# 6 道门禁（sanity_check.py）任一失败 → POST 到此 webhook 推送告警
# 未配置时静默跳过告警（不报错，graceful degrade）
#
# 申请指南（飞书自定义机器人）：
#   1. 打开飞书 → 进入目标群聊（建议建一个"DMP 数据质量告警"专用群）
#   2. 群设置 → 群机器人 → 添加机器人 → 自定义机器人
#   3. 给机器人起名（如 "DMP-Sanity-Bot"）+ 头像
#   4. 安全设置（任选一）：
#      - IP 白名单：填本机/服务器出口 IP
#      - 自定义关键词：建议填 "DMP Sanity Alert"（与 sanity_check.py 消息前缀一致）
#      - 签名校验：本模块未实现，请勿启用
#   5. 完成后复制 Webhook 地址，填入下方 FEISHU_WEBHOOK_URL
#   6. 测试：python3 -c "from scraper.core import sanity_check; print(sanity_check._send_feishu_alert('test'))"
#
# 文档参考：https://open.feishu.cn/document/client-docs/bot-v3/add-custom-bot
FEISHU_WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
