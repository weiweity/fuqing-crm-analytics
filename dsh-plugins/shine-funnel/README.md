# `@shine-mage/dsh-shine-funnel`

FUNNEL 功能包：嵌套人数几何、目录供给。可单独装卸，不改 DSH 上游。

`--plugin on` 默认再 `dsh plugin add` 本包。`--funnel off` 目录／GENERATE 不再提供 FUNNEL。`--plugin off` 会连 workbench 一起关。

几何仍随 workbench bundle。购买频次事实仍在 GSV v5。FastAPI 18082 默认 `SHINE_FUNNEL=on`。
