# 证据与恢复约束

- 事实来源是 FastAPI 状态库确认的成功 run、步骤与证据引用；Skill、模型输出、示例资产和压缩摘要均不能替代。
- 固定 fixture 的条件和数据版本不可由旧记忆修改；不同版本的证据不能混成同一结论。
- 不得从例子复制数字作为本次结果。引用前需有当前成功步骤；无证据、主结果失败或权限撤销时不输出数值事实。
- 剩余步骤、截止时间与资源预算由后端重新读取；压缩、重试或恢复不重置预算。
- 当前 B0 不允许真实业务库、任意 SQL、文件访问、脚本执行、自动记忆写入、审批、导出或消息发送。
